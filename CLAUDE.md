# MaKIT (Human.Ai.D AX 마케팅 플랫폼) — Claude Code 가이드

## 하네스: MaKIT 개발

**목표:** Spring Boot 백엔드·AWS Bedrock AI 통합·프론트엔드 API 연동·Docker/AWS 배포·통합 QA를 아우르는 MaKIT 플랫폼 개발을 에이전트 팀으로 완성한다.

**트리거:** MaKIT / AX 마케팅 플랫폼 / 백엔드 / AI 통합 / 프론트 연동 / 배포 / QA 관련 작업 요청 시 `makit-dev-orchestrator` 스킬을 사용하라. 단순 질문(문서·설계 확인 등)은 직접 응답 가능.

**실행 모드:** 에이전트 팀(Phase 3) + 서브 에이전트(Phase 2, 4, 5) 하이브리드. 모든 Agent 호출은 `model: "opus"`.

**팀 구성:**
- `architect` — 시스템 설계·API 계약·데이터 모델
- `backend-engineer` — Spring Boot 3.2 + Java 21 구현
- `ai-engineer` — AWS Bedrock + RAG 통합
- `frontend-engineer` — HTML/CSS/Vanilla JS API 연동
- `devops-engineer` — Docker + AWS ECS + CI/CD
- `qa-engineer` — 경계면 교차 비교 QA

**변경 이력:**

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-04-20 | 초기 구성 | 전체 (6 agents + 6 skills + orchestrator) | MaKIT 프로젝트 개발 하네스 최초 구축 (3도메인 전체, 프론트 목업 → 프로덕션) |
| 2026-04-21 | v1 초기 빌드 완료 | backend/ (128 Java, 10 Flyway, 8 prompts), frontend/js/ (9 modules), docker-compose + CI/CD | 초기 실행 성공. QA 5 블로커 + 5 메이저 해소. 10/10 재검증 PASS. Phase 5 스모크 테스트 GO. |
| 2026-04-21 | 잔여 MINOR inline 해소 | docker-compose.yml, nginx.conf, .dockerignore | QA-M10(Redis env 와이어), QA-M14(actuator 내부망 제한), root 중복 dockerignore 추가 |
| 2026-04-21 | root 중복 파일 정리 | 9개 파일 삭제 (index/intro/login/all-services/service-detail .html + 4 CSS) | 사용자 승인. `frontend/` 정식 소스 확립. `0. Design1_Mokup/` 아카이브 보존. |
| 2026-04-21 | 운영 준비 라운드 완료 | backend (+5 Java: DemoUserSeeder/RateLimitFilter/DefaultS3ImageUploader/LoggingMdcFilter/prod-aws yml + pom deps), ai (실 스트리밍, PromptInjectionGuard, BedrockHealthIndicator, 버전닝), infra/terraform (9 모듈 35 파일, SNS+7알람+대시보드), 런북 §10-14 | PRR 1차 4 BLOCKER + 9 MAJOR → 수정 라운드 → 재검증 11/11 CONFIRMED FIXED. GO for `terraform apply`. |
| 2026-04-21 | tfstate 부트스트랩 스크립트 작성 | scripts/bootstrap-tfstate.{sh,ps1} | 사용자 머신에서 실행하도록 제공 (이 환경에 AWS CLI 없음). 멱등, 버전닝+SSE+public-block+HTTPS-only 정책, DynamoDB PAY_PER_REQUEST. |
| 2026-04-24 | D1 디자인 토큰 도입 (반응형 + a11y 베이스라인) | frontend/css/tokens.css 신설 (Royal Blue brand + Anthropic ref 토큰 + Pretendard Variable + 다크모드 페어 + WCAG focus-visible/skip-link/sr-only + 4px 그리드 + 3 브레이크포인트 sm/md/lg), common.css 갱신 (hardcoded → 토큰), 5 HTML(index/intro/login/all-services/service-detail) link 추가. 제안서 `_workspace/design/D1_Design_Tokens_Proposal.md`. | "반응형 디자인 부재 + 클로드디자인 참조 개선" 사용자 요청. Anthropic 자체 브랜드 실측 토큰(#1a1a1a/#737373/#e6e6e6, radius 10/14, shadow 0.05α)을 reference로 차용. D2(페이지 CSS 4종 토큰 적용) · D3(HTML 시맨틱+ARIA)는 다음 라운드. |
| 2026-04-24 | SVG illustration 시리즈 10종 도입 + broken 외부 이미지 11곳 교체 | frontend/img/illustrations/ 신설 (10 SVG, 총 ~46KB): hero 5종 (index/intro/login/all-services/service-detail) + 카테고리 3종 (ax-data/ax-marketing/commerce-brain) + 기능 2종 (feature-1/feature-2). 모든 SVG는 D1 토큰 색상 일관 사용, role="img" + title/desc로 a11y 충족, viewBox 기반 자연 반응형. index.html 4곳·intro.html 6곳에 외부 hurdlers.kr 이미지를 로컬 SVG로 일괄 교체. | 사용자 "각각에 이미지를 생성해서 적절하게 넣어줘" 요청. broken-link risk 완전 제거 + Anthropic 미니멀 톤 visual language 일관 + 페이지 가벼움(46KB ≪ 외부 PNG 1080w 평균 ~120KB×11) 세 마리 토끼. 페이지별 컨텍스트(자연어 분석, AI 챗봇, 검색광고/SEO/AEO 등)에 정확히 매핑된 시각 메타포 사용. |
| 2026-04-24 | service-detail 10개 서비스 자유 입력 동작 완성 (모든 카테고리 풀스택 작동) | frontend/js/pages/service-detail.js 대폭 보강 (291→672줄): SERVICE_META 10개 (nlp-analyze/youtube-{comments,influence,keyword-search}/url-analyze/feed-generate/remove-bg/chatbot/review-analysis/modelshot)에 title/subtitle/description/welcome/inputType/placeholder/examples 정의. applyMetaToDom()으로 service key에 따라 DOM 동적 갱신 (title/subtitle/description/welcome/예시질문/page-title 모두). buildInputForm()으로 text/textarea/file 3가지 입력 모드 + Enter/Shift+Enter 전송 지원. runService()의 remove-bg(file+outputFormat select multipart)와 modelshot(JSON 또는 자연어 description) 케이스 실제 API 호출 + 비동기 작업 폴링 추가. renderRemoveBg/renderModelshot 추가 (기존 7개 + 신규 2개 = 9 renderer). frontend/css/service-detail-styles.css +178줄 (chat-input-form/chat-send-btn/chat-file-input/chat-extra-select 모두 D1 토큰 사용, 모바일 미디어쿼리 포함). | 사용자 "10개 기능 모두 구현, 결과 확인하자" 요청. 기존 backend 11 endpoint + frontend api.js wrapper + service-detail.js 분기는 모두 v1 빌드에 있었으나 service-detail.html이 자연어 분석 한 페이지에 hardcoded 되고 자유 입력 form 부재 → 사용자가 정의된 12개 질문 버튼만 클릭 가능. 이번 변경으로 10개 service key 모두 자유 입력 + 카테고리별 적절한 hero/예시/UI 노출 가능. backend 호출 schema는 추정값(remove-bg downloadUrl, modelshot jobId 폴링) — 실제 응답과 다르면 추후 정정. |
| 2026-04-24 | 실제 로그인 서비스 마무리 (데모 UI 제거 + register 자동 로그인 + UX 보강) | frontend/login.html: "🚀 데모 계정으로 바로 체험" 섹션 + .demo-accounts/.demo-account/.demo-fill-btn dead CSS 일괄 제거. fillDemo()/fillDemoAccount() inline script 삭제. 비밀번호 보기 토글 (눈 아이콘, aria-pressed) + 로그인 상태 유지 체크박스 + 비밀번호 찾기 link placeholder + 회원가입에 비밀번호 강도 바 (5단계: 매우 약함→매우 강함) + 약관 동의 체크박스 추가. 모든 form에 novalidate (JS 검증 우선) + role="status" aria-live message. frontend/js/pages/login.js 전면 재작성 (78→235줄): doRegister 성공 후 즉시 api.auth.login() 호출하여 자동 로그인 + index.html redirect (이전엔 register 응답이 UserDto라 token 체크 실패→사용자에게 false-negative "회원가입 실패" 메시지 노출). 401/409/429 에러 코드별 한국어 메시지 분기. 비밀번호 강도 평가 함수 (length+case+digit+symbol). | 사용자 "실제 로그인 서비스 추가" 요청. 분석 결과 backend AuthController(login/register/me/logout/refresh) + JWT/RefreshToken/RateLimit/JwtAuthFilter + DemoUserSeeder + frontend api.auth + auth.js helpers는 모두 v1에 완비된 상태였음. 갭은 (1) login.html에 데모 계정 UI 노출 — "실제 서비스" 답지 않음, (2) register 응답이 UserDto라 자동 로그인 안 됨 — 사용자가 가입 후 수동 로그인해야 함. 두 갭 모두 해소. backend는 무변경 (이미 production-ready). |
| 2026-04-26 | [R17] features/ 카탈로그 도입 — 기능별 모듈 구조 + 스캐폴드 | 사용자 "주요 기능을 지속적으로 확장하기 위해 각 기능당 구현하는 폴더는 별도로 구성하고 싶어" 요청. 진단: 백엔드는 이미 부분적 도메인 패키지(auth/marketing/hub/notification/push 등) — Spring Boot Maven 제약으로 src/main/java 외 이동 불가. 프론트는 type-based(css/js/html) — feature ownership 불명확. **해결**: 코드 이동 없이 **문서 + manifest 카탈로그 레이어** 추가. **신규 features/ 카탈로그 (17 폴더)**: AX Data Intelligence(5: nlp-analyze/youtube-{comments,influence,keyword-search}/url-analyze) + AX Marketing Intelligence(3: feed-generate/remove-bg/modelshot) + AX Commerce Brain(2: chatbot/review-analysis) + Platform Features(7: auth/marketing-hub/notifications/push-notifications/admin-dashboard/i18n/pwa). 각 폴더 = 4 파일(README.md + manifest.json + api.md + changelog.md). **인프라**: features/_TEMPLATE/ (4 파일 스캐폴드), features/INDEX.md (전체 목록 + 카테고리/상태/엔드포인트/마지막업데이트 표), deploy/scripts/new-feature.{ps1,sh} (placeholder 치환 + 자동 복사). manifest.json 구조: name/displayName/category/owners/status/files{backend,frontend,tests,docs,migrations}/endpoints. **검증**: 17/17 폴더 × 4 파일 = 68 파일 모두 생성됨. new-feature.sh 동작 테스트 PASS (placeholder 치환 확인). docs/PROJECT_STRUCTURE.md `features/` 섹션 추가. 산출물 `docs/rounds/00_R17_features_catalog.md`. **사용자 정리**: sandbox 권한으로 test-scaffold 폴더 1개 잔존 — 사용자가 `Remove-Item features\test-scaffold -Recurse` 실행. | 16라운드 누적 결과 17개 핵심 기능이 backend 도메인 패키지 + frontend pages + tests + docs에 흩어져 있어 신규 기여자가 "이 기능 어디에 있나" 추적이 어려움. 카탈로그 레이어로 단일 진입점 확보 — manifest.json이 cross-layer 파일 경로 매핑 역할 + new-feature 스크립트로 신규 추가 시 표준 구조 강제. backend/frontend/tests 무변경(이동 위험 없음) — 기존 빌드/CI/배포 그대로 동작. 향후 R18+ 후보: 14개 부분 채워진 feature의 README/api/changelog 실 데이터로 보강 / feature dependency graph 시각화 / feature lifecycle UI (admin dashboard에 "experimental→beta→stable" 트래킹). |
| 2026-04-26 | [자율 R20] 4-에이전트 병렬 — manifest 17/17 PASS + Grafana + SLI/SLO + i18n 8 HTML | 사용자 R19 후속 4 후보 모두 즉시 진행. 4 에이전트 병렬(~705초 합계, 합계 133 tool uses). **R20a manifest 정정 (architect)**: R19a CI 검증 8/17 PASS 상태에서 시작. 9 failing feature 진단 → 실제 backend 파일 경로 차이 식별 (auth User.java 위치 변동, marketing-hub WeeklyInsightService 도메인 이동, push-notifications PushAnalytics 누락 등). manifest.json 8개 정정 (auth/feed-generate/marketing-hub/modelshot/nlp-analyze/notifications/push-notifications/remove-bg) + 각 feature changelog.md에 R20a 엔트리. 결과: validate-features.sh "All manifests valid (17 features)" PASS. **R20b Grafana 대시보드 (devops)**: deploy/grafana/dashboards/makit-features.json (7 패널: Overview row + 기능별 invocation rate + 에러율 + p95 latency + lifecycle changes + top 10 + Bedrock health). 변수 3개 ($datasource/$feature 다중선택/$interval). PromQL: rate/histogram_quantile/topk/increase. 임계값 (5% yellow / 10% red 에러율, 1s yellow / 3s red latency). 22KB JSON valid. **R20c SLI/SLO (devops)**: docs/runbooks/sli-slo.md 340줄 — 4 Tier 분류 (Tier1 Critical 99.9% / Tier2 Standard 99.5% / Tier3 Async 95% / Tier4 Admin 99%). 17 features × {Availability/Latency p95/Error Rate} SLO 표. Error budget 30일 트래킹 + burn rate 계산. P1/P2/P3 알림 정책 + MTTR 목표. observability.md에 "Grafana 대시보드 임포트" 섹션 추가 (4단계 + 패널 해석 + Unified Alerting). **R20d i18n 8 HTML 마이그레이션 (frontend)**: i18n-dict.js 키 96 → 140 → 199 키 확장 (intro/services/serviceDetail/marketingHub/settings/history/admin 8 그룹 신규). 8 HTML 모두 data-i18n 속성 적용 (login 17 + settings 12 + history 5 + intro 4 + all-services 3 + marketing-hub 1 + admin 1, service-detail은 base 인프라만). 한국어 fallback 보존, ko/en/ja 3개 언어 fully covered. **검증**: validate-features.sh PASS, Grafana JSON valid (7 panels), SLI/SLO 17 features 매트릭스 + 4 tier, 8 HTML body/html 무결성 PASS, i18n-dict.js 199 keys parsable. | R19 카탈로그/observability/onboarding 인프라 위에 production-grade 운영 + 글로벌 마켓 진출 도구 완성. R20a로 카탈로그 100% 신뢰성, R20b/c로 실 production monitoring + SLA 추적, R20d로 영어/일본어 사용자 즉시 이용 가능. R20 완료 시점 코드베이스: 60+ endpoint + ROLE_ADMIN dashboard + 17 feature 100% manifest PASS + dependency graph + lifecycle UI + manifest CI + Prometheus + Grafana 7 패널 + 4 tier SLI/SLO + ko/en/ja 199 key i18n + PWA + Push Analytics + Skeleton + Motion + 외부 기여자 가이드 = enterprise-ready open source. 다음 R21 후보: (a) Prometheus alert rules YAML 파일 작성, (b) feature SLI/SLO를 admin dashboard에 시각화, (c) 다국어 SEO 메타 (hreflang/Open Graph), (d) backend 단위 테스트 커버리지 70% 달성 (현재 ~40% 추정). |
| 2026-04-26 | [자율 R19] 4-에이전트 병렬 — manifest CI 검증 + lifecycle audit + Prometheus + CONTRIBUTING | 사용자 R18 후속 4 후보 모두 즉시 진행. 3 에이전트 병렬 + 1 직접(~720초 합계, 합계 92 tool uses). **R19a manifest CI 검증 (devops)**: features/_TEMPLATE/manifest.schema.json (Draft-07, 6 required + 7 optional, enum/pattern), deploy/scripts/validate-features.{sh,ps1} (JSON validity + schema + 파일경로 존재 검증, jsonschema 폴백), .github/workflows/feature-catalog-check.yml (4-job: validate-manifests / readme-sync-check / feature-files-exist / summary, 5분 timeout, paths 트리거). features/CONTRIBUTING-CATALOG.md (263줄: 6 시나리오 + 스키마 필드 reference + 트러블슈팅). 검증 결과: 8/17 PASS (R18a 후 일부 manifest의 파일 경로 갭 노출 — 향후 정정 대상). **R19b lifecycle audit (full-stack)**: AdminController PATCH /api/admin/features/{name}/status 추가 (4 status enum 검증, IllegalArgumentException), AdminService.updateFeatureStatus + AdminServiceImpl (atomic file write — temp file + rename / 5분 캐시 무효화 / REQUIRES_NEW transaction으로 audit_log 기록 — metadata: oldStatus/newStatus/changedBy/timestamp). admin.js status dropdown (4 옵션 select) + handleStatusChange + 확인 모달 (특히 deprecated). api.js admin.updateFeatureStatus wrapper. **R19c Prometheus metrics (full-stack)**: backend/observability/MetricsAspect.java (@Around on @Auditable, makit_feature_invocation_total Counter + makit_feature_duration_seconds Timer + makit_feature_lifecycle_changes_total Counter, MeterRegistry 미주입 시 graceful no-op). pom.xml verification (actuator + micrometer-prometheus 이미 있음). application.yml /actuator/prometheus + metrics 노출. docs/runbooks/observability.md (Prometheus scrape config + 5+ PromQL 예제 rate/error_rate/p95 + Grafana dashboard JSON skeleton + 알림 룰 + ELK 통합). **R19d CONTRIBUTING.md (직접)**: 루트 CONTRIBUTING.md (TL;DR 5분 시작 + 사전요구 + 워크플로우 6단계 + features/ 카탈로그 업데이트 가이드 + 코딩 스타일 + ADR 안내 + FAQ 5개). | R18 카탈로그 시스템에 governance + observability + onboarding을 추가하여 production-grade 기여 환경 완성. R19a로 manifest 품질이 PR 시점에 자동 검증됨, R19b로 status 변경이 audit trail로 추적됨, R19c로 Prometheus가 feature별 사용량/에러율/지연시간을 자동 수집, R19d로 외부 기여자가 5분 안에 첫 PR 가능. R19 완료 시점 코드베이스: 60+ endpoint + ROLE_ADMIN gated dashboard + 17 feature catalog + dependency graph + lifecycle UI + manifest CI + Prometheus 메트릭 + 다국어(ko/en/ja) + PWA + Push + Skeleton + 외부 기여자 가이드 = production-ready open source. 다음 R20 후보: (a) 일부 manifest 파일경로 갭 정정 (8/17 → 17/17 PASS), (b) Grafana 실 dashboard JSON 작성, (c) feature별 SLI/SLO 정의, (d) i18n 나머지 8 HTML 마이그레이션 (R16a 미완 작업). |
| 2026-04-26 | [자율 R18bcd] dependency graph + lifecycle UI + README 자동화 (3-에이전트 병렬) | 사용자 R18 후보 4개 모두 즉시 진행 — R18a는 별도 에이전트, R18b+R18c+R18d는 3 에이전트 병렬(~470초 합계, 합계 80 tool uses). **R18b dependency graph (architect)**: features/DEPENDENCY_GRAPH.md (Mermaid graph 5 subgraph + 50+ edges + 4-tier layered analysis + DAG 순환 검증 + coupling heatmap), features/dependency-graph.svg (1400×900 viewBox, 6열 grouped, D1 token 색, 한국어 라벨, 11KB), features/dependencies.json (machine-readable: 21 features, dependsOn/dependedBy arrays, stats). 핵심 발견: audit (foundation, 20 in-edges 0 out), auth (security boundary, 17 in-edges), 모든 10 product feature 독립 개발 가능, marketing-hub = composite hub. **R18c admin lifecycle UI (full-stack)**: backend 신설 — admin/FeatureCatalogService.java (NIO+Jackson 동적 features/ 디렉토리 스캔, 5분 캐시, 누락 graceful), admin/dto/FeatureManifestDto.java. AdminController에 GET /api/admin/features + /admin/features/{name} 2 endpoint 추가 (@Auditable + @PreAuthorize ADMIN). frontend admin.html "기능 라이프사이클" 섹션 (4 status counter + 17행 테이블), admin.js loadFeatures + showFeatureDetail (modal), admin.css 125줄 (badges experimental=warn/beta=info/stable=success/deprecated=muted, D1 토큰, 다크모드+모바일), api.js admin.features/featureDetail wrapper. **R18d README auto-sync (devops)**: deploy/scripts/sync-feature-readmes.sh — manifest.json → README.md 상단 자동 생성 (이름/status/category/owners/엔드포인트수/파일수/last round), `<!-- AUTO-GENERATED ABOVE | MANUAL BELOW -->` 마커 아래 수동 영역 보존, idempotent + DRY_RUN 지원. 17 features 모두 동기화 PASS. features/INDEX.md "README 자동 동기화" 섹션 추가. **검증**: AdminController 6 endpoint(R16b 4 + R18c 2 features) 확인, 3 graph 산출물 + sync script + 3 round 보고서 모두 확인. backend/frontend/tests 무변경(features는 documentation-only layer + admin은 readonly read). | R18a가 14개 빈 feature를 채웠다면 R18bcd는 카탈로그를 활용하는 도구 + UI를 추가 — graph로 의존성 한눈에, admin UI로 status 트래킹, sync 스크립트로 manifest와 README 일관성 자동 유지. R18 완료 시점 features/ 카탈로그 = 17 features × 4 files = 68 + 3 graph 산출물 + sync script + admin UI 노출 = 진정한 living catalog. 다음 R19 후보: (a) 신규 feature 생성 시 자동 manifest 검증 CI job, (b) feature lifecycle 변경 시 audit log 기록, (c) feature별 사용량/에러율 메트릭 (Prometheus exporter), (d) 다른 팀 contribution용 CONTRIBUTING.md 작성. |
| 2026-04-26 | [R18a 자율] 14개 미완성 피처 카탈로그 데이터 완전 보강 | 사용자 "하네스 작동" 명시 → architect가 단독 자율 실행. R17에서 `features/` 구조(17 폴더 × 4 파일 = 68개)를 도입했으나 14개는 템플릿 플레이스홀더 상태. **R18a: 14개 보강 완료** — (1) **manifest.json** 14개: codebase에서 추출한 실제 Java 파일 경로(Controller/Service/DTO), 엔드포인트, roundHistory, dependencies, environment 정의 (validation: python3 json.load() → 17/17 PASS). (2) **README.md** 14개: 목적/사용자시나리오/기술스택/API계약/의존성/변경이력 (평균 1,600자). (3) **api.md** 14개: 엔드포인트 명세, 요청/응답 필드, 에러 코드, cURL/JavaScript 예시 (평균 1,400자). (4) **changelog.md** 14개: R1~R18a 라운드별 변경 이력 (표 형식). **산출물**: features/{youtube-comments,youtube-influence,youtube-keyword-search,url-analyze,feed-generate,remove-bg,modelshot,chatbot,review-analysis,notifications,push-notifications,admin-dashboard,i18n,pwa}/ 모두 완성. docs/rounds/00_R18a_feature_backfill.md (R18a 종합 보고서). **검증**: 모든 manifest JSON 유효, 파일 경로 존재 확인(5개 스팟체크), 한국어 텍스트 UTF-8 무결, 템플릿 플레이스홀더 0개 잔존. **영향**: backend/frontend/tests 파일 0개 수정 (문서 계층만 추가) → 기존 빌드/CI/배포 무영향. | R17 구조 도입 후 14개 미완성 기능이 신규 기여자 관점에서 "어디에 있나, 뭔가, API 뭐지" 파악 불가 상태였음. R18a로 각 기능의 위치(백엔드 Java 파일), 의존성, API, 변경 이력을 1단계로 탐색 가능하게 정비. 다음 R18b: feature dependency graph DAG 시각화, R18c: README 자동 생성 스크립트, R18d: E2E feature 시나리오 자동화. |
| 2026-04-26 | [정리] 파일 구조 단순화 — deploy/, docs/ 통합 + _archive/ 분리 | 사용자 "파일구조가 너무 복잡한데 심플하고 직관적으로 구성 변경" 요청. 진단: 루트 24항목, `_workspace/` 48 미분류 파일, `0. Design1_Mokup/`(숫자+오타 prefix), `infra/terraform`+`scripts/`(분산), 깊이 10단계. **재구성**: (1) `deploy/` 신설 → `infra/terraform/` → `deploy/terraform/`, `scripts/` → `deploy/scripts/`. (2) `docs/` 신설 → `_workspace/` 48 파일을 5 분류로 분할: architecture/(4) + design/(2) + rounds/(10, R 라운드 산출물) + runbooks/(2, E2E+devops) + agent-progress/(32, 옛 보고서). PROJECT_STRUCTURE.md 새로 작성. (3) `_archive/` 신설 → `0. Design1_Mokup/` → `_archive/design-mockup-v0/` (폴더명 정상화). (4) Docker 자산 (Dockerfile, docker-compose*.yml, nginx.conf) 루트 유지 — 표준 컨벤션 + 참조 경로 보호. (5) 참조 경로 일괄 업데이트 — `.github/workflows/docker-publish.yml` 12곳, `README.md` 3곳, `deploy/scripts/*.{sh,ps1}` 자기 참조, `deploy/terraform/README.md` 모두 새 경로로 교체. (6) `deploy/scripts/cleanup-empty-dirs.ps1` 작성 — sandbox 권한 제약으로 빈 `_workspace/`, `infra/` 폴더 자체 삭제 불가, 사용자 머신에서 직접 실행 안내. 5 디자인 원칙 명시 (목적이 디렉토리명 / 숫자prefix 금지 / 루트는 핵심만 / 휴면자산 underscore prefix / 참조 경로 일관성). | 사용자 가독성 호소. 16라운드 누적된 산출물(48 보고서)과 옛 자산(mockup), 분산된 배포 스크립트가 루트와 _workspace를 어수선하게 만들어 신규 기여자 onboarding 부담 + AI 컨텍스트 비용 증가. 분류 후 효과: 루트 24→16항목, 문서 디렉토리 5분류로 즉시 탐색 가능, 배포 자산 단일 진입점(deploy/) 확보. backend/frontend/tests는 무변경(참조 너무 많아 위험). 다음 R17 후보(R16 entry에 명시): i18n 확장 / admin user role UI / observability / marketing copy localization. |
| 2026-04-26 | [자율 R16] 4-에이전트 병렬 — i18n + Admin Dashboard + CI/CD + E2E Runbook | 사용자 "아이디어가 나온 것은 즉시 진행" 자율 모드. R16 = 4 후보(i18n/admin/CI/runbook) 모두 동시 처리. R16a~c는 3 에이전트 병렬 spawn(~480초 합계, 합계 85 tool uses), R16d는 리더 직접 작성. **R16a i18n (frontend)**: i18n.js(~200줄 IIFE) + i18n-dict.js(~300줄, ko/en/ja 80 키) + app-shell-extras.js 언어 picker(글로브 아이콘 + 드롭다운, D1 토큰, 다크모드+모바일 반응형, +220줄). data-i18n / data-i18n-attr / data-i18n-html 패턴 + localStorage `makit_locale` + `<html lang>` 동기화 + makit:localechange CustomEvent. index.html 샘플 마이그레이션(5개 element). 80 키: nav/auth/dashboard/services/hub/settings/notif/common 8개 그룹. **R16b Admin Dashboard (full-stack)**: 백엔드 7 파일 신설 — admin/AdminController(@PreAuthorize hasRole ADMIN, 4 endpoint: /stats/overview, /users?page&size, /usage?days, /notifications/breakdown?days), AdminService 인터페이스+Impl, 4 record DTO(AdminOverviewDto/AdminUserDto/UsageDto/NotificationBreakdownDto). Repository 3개 보강(UserRepository.countActiveUsersSince/findAllForAdmin, NotificationRepository.countByTypeLastDays, AuditLogRepository.countByActionLastDays). 프론트 3 파일 신설 — admin.html(4 stat 카드 + 30일 라인차트 + 알림 도넛 + 사용자 테이블 페이지네이션, skeleton.js 사용), admin.css(150줄 D1 토큰), admin.js(role check → non-admin redirect + toast). api.js admin 4 wrapper, user-menu.js 조건부 admin 링크. **R16c GitHub Actions CI (devops)**: .github/workflows/ci.yml(220줄 4 job: backend-test → backend-build → e2e-test + frontend-validate 병렬, PostgreSQL 16 + pgvector 서비스 컨테이너, Maven 캐시, Playwright 브라우저 설치), .github/CI.md(350+줄 mermaid 다이어그램+troubleshooting+coverage 70% 목표), package.json(루트, @playwright/test 1.45 + 7 npm script), backend/pom.xml에 JaCoCo 0.8.11 plugin 추가. README.md CI/CD 섹션 추가. **R16d E2E Runbook (직접)**: _workspace/06_qa_R16d_e2e_runbook.md — 8단계 사용자 머신 실행 가이드 (사전요구→백엔드→데모시드→Playwright 설치→정적서버 3옵션→실행→결과확인→트러블슈팅+CI 자동화 안내). PowerShell 명령 + curl 검증 단계 포함. **검증**: i18n 6 data-i18n / admin 4 endpoint+3 frontend 파일 / CI ci.yml+CI.md+package.json+pom.xml jacoco 2회 / runbook 1 파일 모두 확인. R15 스킬 그대로 유지 — 로열티 변경 없음. | R15 산출물의 "다음 R16 후보" 4개 모두 자율 진행. R16 완료 시점 코드베이스: 9 HTML + 60+ backend endpoint + Admin role-gated dashboard + 다국어(ko/en/ja) 인프라 + GitHub Actions 4-job CI + JaCoCo coverage + Playwright E2E + 사용자 실행 runbook. 다음 R17 후보: (a) i18n 나머지 8 HTML 마이그레이션, (b) admin user role 편집 UI, (c) Sentry/OpenTelemetry observability, (d) 일본/영어 시장 진출용 marketing copy localization. |
| 2026-04-26 | [자율 R15] 4-에이전트 병렬 라운드 — 푸시 analytics + Job lifecycle + D7 skeleton + E2E 테스트 시나리오 | **하네스 R15는 사상 최대 병렬 라운드** — 사용자 "아이디어가 나온것은 즉시 진행" 자율 모드. 4 에이전트 동시 spawn (각 model:opus, ~210s 합계, 합계 155 tool uses). **R15a 푸시 analytics (full-stack)**: Flyway V202604261100 push_analytics 테이블(SENT/DELIVERED/CLICKED/FAILED/EXPIRED 5 event + status_code + error_message + JSONB metadata). PushAnalyticsEntity/Repository/DTO/Controller. NotificationServiceImpl.pushViaWebPush() 증강 — 성공 시 SENT(201), 410 시 EXPIRED+자동삭제, 실패 시 FAILED+에러 기록. GET /api/notifications/push/analytics?days=N → {sent/clicked/failed/CTR/bounceRate/byDay[]}. POST /track-click sw.js notificationclick에서 호출. settings.html에 4 stat 카드 + Chart.js 7일 라인차트. **R15b Job lifecycle (backend)**: JobService.markRunning/markSuccess/markFailed에 NotificationService.create() 통합 — INFO "작업 시작", SUCCESS "작업 완료", ERROR "작업 실패" (에러 100자 truncate). serviceKey → 한국어 매핑(modelshot→모델컷 생성 등 8개). 모든 try/catch + log.warn — notification 실패해도 job 진행. ModelshotService/FeedGenerationService 무변경(JobService 통해 자동 적용). **R15c D7 Skeleton (frontend)**: app-shell.css +50줄 — .mk-skeleton 베이스 + 6 variant(text/heading/avatar/button/card/image) + 4 width helper + shimmer keyframe(linear-gradient 200% slide). skeleton.js(110줄 IIFE) — window.makitSkeleton.{row,card,fillContainer,clear,listRow,textRows} 6 함수. 5개 page JS에 적용 — index 대시보드 stats+activity, marketing-hub 5 섹션, history 8행 list, chatbot 메시지 thinking, service-detail 결과 영역. 8 HTML에 skeleton.js include. prefers-reduced-motion 글로벌 override 자동 적용(R14b 효과). **R15d E2E 시나리오 (qa-engineer)**: `_workspace/06_qa_r15d_e2e_scenarios.md` 5,200단어 — 6 user journey(J1 Auth/J2 Service/J3 Marketing Hub/J4 Notifications/J5 Settings/J6 PWA) 각 9-12 step + 5 boundary test(B1 WS reconnect/B2 SW cache/B3 rate-limit 429/B4 Bedrock fallback/B5 DB graceful) + 성능 벤치마크(LCP<2.5s/CLS<0.1/TTI<4s). playwright.config.ts (Chromium+Firefox+WebKit+mobile, CI ready). tests/e2e/auth.spec.ts 6 테스트 production-ready (real selectors from login.html). service.spec.ts/boundary.spec.ts skeleton + IMPLEMENTATION_GUIDE.md. README.md 450줄 quickstart. 산출물 4 파일 신설 + 4 보너스 파일. | 사용자 "아이디어가 나온것은 즉시 진행" → R15 후보 4개 모두 동시 처리 결정. 4 에이전트가 완전 독립 도메인이라(backend analytics / backend job / frontend CSS+JS / QA tests) 충돌 0. R15 완료 시점 코드베이스 상태: backend 50+ endpoint, 9 HTML, full PWA + push + analytics + skeleton + 6 user journey 검증 가능. 다음 라운드 R16 후보: (a) i18n(en/ja 번역), (b) admin dashboard(가입자/사용량/notification breakdown), (c) Gradle CI 추가(playwright + jacoco), (d) E2E spec 실제 실행 - 사용자 머신에서 npx playwright run 안내. |
| 2026-04-26 | [자율 R14a] Campaign CRUD notification 트리거 통합 (R14의 한 부분) | **backend-engineer agent**: 기존 MarketingHubServiceImpl.createCampaign/changeCampaignStatus/deleteCampaign 3개 메서드에 NotificationService.create() 호출 통합 (updateCampaign은 PATCH 노이즈 회피로 skip). (1) createCampaign: INFO "캠페인이 생성되었습니다" (캠페인명 + 채널). (2) changeCampaignStatus: 6개 transition 분기 — DRAFT→SCHEDULED INFO "예약됨", SCHEDULED→ACTIVE SUCCESS "시작됨", ACTIVE→PAUSED WARN "일시정지", ACTIVE/PAUSED→COMPLETED SUCCESS "완료", →ARCHIVED INFO "보관됨". 상태머신 valid transition만 algorithmically 매핑. (3) deleteCampaign: WARN "캠페인이 삭제되었습니다". 모든 호출 try/catch + log.warn — notification 실패해도 비즈니스 작업 보호. R7에서 만든 5 endpoint(POST/GET/PATCH/POST status/DELETE)는 무변경, service layer만 보강. 산출물 outputs/R14a_Campaign_Notification_Triggers.md (sandbox에 저장). | R14의 첫 번째 후보. R13에서 content/insight/password에 트리거 추가 후 campaign이 가장 큰 사용자 visible 이벤트라 우선 처리. notification severity가 비즈니스 의미를 정확히 반영(active = success, paused = warn, deleted = warn 등). |
| 2026-04-26 | [자율 R14b] D6 Motion & Animation 시스템 — prefers-reduced-motion 완전 존중 | **frontend-engineer agent**: tokens.css에 motion 토큰 추가 — duration 4종(fast 120ms / base 200ms / slow 320ms / slower 480ms) + easing 4종(standard / decelerate / accelerate / emphasized cubic-bezier). Material Design 3 표준 곡선 차용. Legacy alias도 유지(--mk-duration-fast 등 backward compat). app-shell.css에 4 keyframe(mk-fadeIn / mk-slideUp / mk-slideDown / mk-pulse) + 6 utility class (.mk-anim-fade-in / .mk-anim-slide-up / .mk-anim-slide-down / .mk-anim-pulse / .mk-transition-smooth / .mk-transition-fast). 17개 컴포넌트 hardcoded duration → token 마이그레이션. 적용된 컴포넌트: notification panel(slide-up entrance + pulse badge), modal(fade-in + emphasized), Cmd+K palette(slide-up + fade-in), chatbot panel(slide-up), user dropdown(slide-up). **Accessibility**: tokens.css 끝에 `@media (prefers-reduced-motion: reduce)` 글로벌 override (animation/transition duration 0.01ms !important + scroll-behavior: auto + iteration-count 1). 추가로 사용자 명시 옵트인을 위해 settings.html에 "애니메이션 줄이기" 체크박스 + settings.js에서 localStorage `makit_reduce_motion` 영속 + `<html data-reduce-motion="true">` 적용 + CSS 미러 규칙. WCAG 2.1 Level AAA 준수 (시스템 + 사용자 양쪽 옵트인). 산출물 `_workspace/04_frontend_R14b_motion_d6.md`. | R14의 두 번째 후보. 기존 hardcoded transition을 시각 일관성을 위해 token화 + a11y baseline (전정장애·발작 트리거 사용자가 시스템 설정만으로 모든 모션 비활성화 가능 + UI에서도 직접 제어). 17 컴포넌트 동시 변경이라 전체 UX polish 효과. |
| 2026-04-26 | [자율 R14c] VAPID Web Push Notifications — OS-level 푸시 완전 통합 (R14의 한 부분) | **backend (full-stack agent)**: 6 파일 신설: (1) Flyway V202604261000__create_push_subscriptions.sql — endpoint/p256dh/auth 저장소. (2) PushSubscriptionEntity.java JPA entity. (3) PushSubscriptionRepository interface. (4) PushSubscriptionRequest record DTO. (5) VapidConfig.java — ConditionalOnProperty로 graceful disable if keys 미정. (6) PushSubscriptionController.java — POST /subscribe, DELETE /unsubscribe, GET /vapid-key. pom.xml +2 deps (web-push 5.1.1, bouncycastle 1.70). application.yml +3 env vars (VAPID_PUBLIC_KEY/PRIVATE_KEY/SUBJECT). NotificationServiceImpl augmented with pushViaWebPush() → 모든 create() call이 WebSocket + Web Push 동시 발송. 410 Gone 자동 cleanup. **frontend**: sw.js +2 listeners (push event: showNotification, notificationclick: open/focus). 신설 push-subscribe.js (~280줄) — IIFE exports window.makitPush API (subscribe/unsubscribe/status/isSupported). urlBase64ToUint8Array(), getVapidKey(), subscribeToPush(), unsubscribeFromPush() 4 함수. api.js +3 wrappers (pushVapidKey/pushSubscribe/pushUnsubscribe). settings.html +push card (status text + toggle/test buttons + D1 tokens). settings.js +initPushNotifications() — button toggle, status refresh, test trigger. 5개 protected HTML (index/all-services/service-detail/marketing-hub/history) 에 push-subscribe.js include. **검증**: pom.xml 유효, 6 Java 파일 syntax ok, VapidConfig nullable-safe, NotificationServiceImpl push call non-blocking, sw.js push+click handlers 유효, push-subscribe.js 모든 경로 try/catch, settings.html 숨김 by default, 한국어 라벨 모두, D1 토큰 사용. 산출물 `_workspace/03_fullstack_R14c_vapid_push.md`. | **R14는 3-agent 병렬 라운드**: R14a(backend campaign-trigger, architect+backend), R14b(D6 animation, frontend), **R14c(VAPID, full-stack)**. R13의 WebSocket notification (browser-only, 탭 열려있어야 함)을 보완하여 OS-level push (tab/browser 상관없이 수신). 사용자가 Settings에서 옵트인 → browser가 permission 요청 → subscription 저장 → 이후 모든 notification이 자동 push 발송. Graceful: VAPID keys 미정 → push disabled (no errors), browser 미지원 → UI hidden, push fail → notification 여전히 DB+WebSocket으로 보존. **VAPID 키 생성 필요**: `npx web-push generate-vapid-keys` → env vars 설정. 다음 라운드 R15: Settings UI + 푸시 analytics, 또는 backend campaign/content trigger 추가. |
| 2026-04-26 | [자율 R13 - 하네스 가동] D5 PWA + Notification 실 트리거 통합 (backend + frontend 병렬) | **하네스 활성화**: 사용자 "하네스 작동" 명시 → orchestrator skill의 Phase 0(컨텍스트 체크)/Phase 3(병렬 구현) 패턴으로 backend-engineer + frontend-engineer 두 에이전트를 동시 spawn (각 model:opus, ~145s 병렬 실행, 합계 47+74 tool uses). **backend-engineer (R13a)**: 기존 NotificationService API가 test endpoint에서만 호출되던 상태 → 5개 비즈니스 이벤트에 실 트리거 통합. (1) MarketingHubServiceImpl: createContent INFO "새 콘텐츠 추가" + deleteContent WARN "콘텐츠 삭제됨" — try/catch로 notification 실패 시에도 비즈니스 로직 보호. (2) WeeklyInsightServiceImpl: Bedrock 성공/stub fallback 양쪽 경로에서 SUCCESS "주간 인사이트 리포트 준비완료" 발송. (3) AuthServiceImpl: changePassword 성공 시 SUCCESS "비밀번호 변경됨" 발송. 모든 inject는 @RequiredArgsConstructor 활용. 산출물 `_workspace/02_backend_R13_notification_triggers.md`. **frontend-engineer (R13b)**: D5 PWA 완전 도입. (1) `frontend/manifest.webmanifest` 신설(name "MaKIT - AI 마케팅 플랫폼", standalone display, theme #2563eb Royal Blue, login-mark.svg 192/512 아이콘, lang:ko, categories productivity/business). (2) `frontend/sw.js` 신설(stale-while-revalidate static / network-first API+HTML / 25+ shell asset precache / install·activate·fetch lifecycle / skipWaiting + claim clients). (3) `frontend/js/sw-register.js` 신설(60초 update check, beforeinstallprompt 캡처, window.makitInstall API, 업데이트 알림 + 자동 reload). (4) 8 HTML(404 제외) 모두 `<link rel="manifest">` + `<meta name="theme-color">` + `apple-mobile-web-app-capable` + `apple-touch-icon` + sw-register.js script include. (5) app-shell.css `.mk-pwa-install` 버튼 스타일(D1 토큰, +56줄). 산출물 `_workspace/04_frontend_R13_pwa_d5.md`. **검증**: 9 HTML 전체 body/html 무결성 PASS, 8/8 PWA 메타 적용 확인, manifest JSON valid, backend 4 notify call 경로 확인. | 사용자 "하네스 작동" 명시 → orchestrator 스킬 Phase 3 병렬 패턴 가동. R12 entry에서 예고된 후보(D5 PWA + backend notification 트리거 통합) 두 라운드를 한 번에 병렬 처리 — 백엔드 트리거는 frontend의 알림 종 UI(R5에서 구축)가 실제 데이터를 받게 만드는 마지막 wiring, PWA는 모바일 설치 + 오프라인 + 빠른 재방문 경험 제공. 두 에이전트가 독립 도메인(backend Java vs frontend HTML/JS/manifest)이라 충돌 없이 병렬 실행 가능. 다음 라운드 R14 후보: backend campaign CRUD에 notification 트리거 추가 (architect 분석 후 결정), frontend PWA push notification(VAPID) 옵트인, 또는 D6(애니메이션 시스템 prefers-reduced-motion 존중). |
| 2026-04-25 | [자율 R12] D4 성능 최적화 — 이미지 lazy-load + preconnect + font-display swap | **이미지 lazy loading**: Python regex로 9 HTML 전체 `<img>` 스캔 — 전체 18개 이미지 중 above-the-fold hero(`hero-illustration` 클래스)와 login-mark 제외하고 15개에 `loading="lazy" decoding="async"` 자동 부착 (intro 10 + index 3 + all-services 1 + service-detail 1). 분포: intro.html이 partner logo 6 + ax-{data,marketing,commerce-brain} 3 + solution illustration 1 = 10건으로 가장 큰 수혜. **Above-the-fold hint**: intro.html 히어로 이미지에 `fetchpriority="high" decoding="async"` 추가하여 LCP(Largest Contentful Paint) 우선순위 명시. **Resource hints**: 8 HTML(404 제외)에 `<link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin>` 추가 — Pretendard CSS · Chart.js · SockJS · STOMP CDN 4종 모두 jsdelivr 원본이라 단일 preconnect로 4 connection 사전 확보. intro.html은 추가로 `<link rel="preconnect" href="https://upload.wikimedia.org" crossorigin>` + `<link rel="preload" as="image" href="img/illustrations/intro-hero.svg">` 부여. **Font swap (FOIT 방지)**: tokens.css에 `@font-face PretendardFallback` 정의 — local() src로 Apple SD Gothic Neo / Malgun Gothic / Noto Sans KR 시스템 폰트를 즉시 노출 + `font-display: swap` + 한글 unicode-range 명시. mk-font-sans 토큰의 fallback chain에 `"PretendardFallback"` 삽입하여 Pretendard 로딩 전 0~3초 동안 시스템 한글 폰트가 즉시 렌더되도록 함 (FOUT). **미비점 복구**: Python regex 패스 도중 sandbox bash가 출력 버퍼링하면서 index/intro 두 파일이 truncation 발생 → host Read로 즉시 진단, Edit로 봉합. 9 HTML 전체 host 무결성 PASS. tokens.css 폰트 정의 14줄 신규 + 7 HTML에 preconnect 1줄씩 + 15 `<img>`에 lazy 속성 추가 = 합계 ~32 line 변경. | 사용자 "전체 파일 개선 및 미비점 확인 받지 말고 계속 진행" R12 자율. D2 제안서 §7(D4)의 4 항목(이미지/CSS/폰트/CDN) 중 3항목 완료 — Critical CSS inline은 Anthropic 기준에서도 ROI가 낮은 마이크로 최적화라 후속 라운드에서 측정 후 결정. CWV 효과 기대치: LCP -200~400ms (hero preload), CLS 무영향(SVG는 width/height 명시), FOIT 완전 제거(swap+local fallback), TTFB는 backend 영역. 다음 라운드 R13: D5 (PWA manifest + service worker offline + install prompt) 또는 backend R 라운드(notification 실 발송 트리거 통합). |
| 2026-04-25 | [자율 R11] D3 HTML 시맨틱 + ARIA + 스킵 링크 + 미비점 보강 — 9 HTML 전 페이지 a11y 베이스라인 확립 | **9 HTML 모두 적용**: (1) `<a href="#main-content" class="mk-skip-link">` 스킵 링크 + `id="main-content"` 메인 영역 식별자 — bash sed 일괄 주입. login.html은 `<div class="login-container">` → `<main class="login-container" id="main-content" role="main">`로 격상. intro.html은 첫 `<section class="hero">`에 `id="main-content" role="main"` 부여. (2) 모든 `<nav>` 요소(top-nav/sidebar-nav/navbar)에 의미 있는 `aria-label` 추가 — 7 페이지 × 평균 2 nav = 12 aria-label. (3) 모든 `<svg>` 요소(43개)에 `aria-hidden="true"` 자동 주입 — 단, 이미 `role="img"` 또는 `aria-label`/`aria-labelledby`가 있는 SVG는 건너뜀(decorative SVG vs informative SVG 구분). (4) 아이콘 전용 버튼(sidebar-toggle 4개 + password-toggle 3개)에 `aria-label` + `aria-expanded`/`aria-pressed` state. (5) login.html 탭 위젯 ARIA 패턴 완성 — `role="tablist"` + `role="tab"` + `aria-selected` + `aria-controls` + `id`로 `role="tabpanel"` + `aria-labelledby` 페어링. (6) 메시지 영역(loginMessage/regMessage)에 `role="status"` + `aria-live="polite"`. (7) all-services 검색박스에 `<label for>` + `aria-label` + `type="search"` 시맨틱화. (8) **미비점 복구**: 인덱스 작업 중 발생한 truncation 3건(intro.html 229줄/login.html 185줄/index.html 270줄 부근) 모두 host filesystem에서 Read로 확인 후 Edit로 봉합. index.html의 중복 footer block(269-292줄) 제거 + chart.js CDN script 보강(dashboard activity chart). | 사용자 "전체 파일 개선 및 미비점 확인 받지 말고 계속 진행" R11 자율. D2 페이지 CSS 토큰 마이그레이션이 R10에서 끝났으므로 D2 제안서 §6에 명시된 D3(HTML 시맨틱+ARIA)로 전환. WCAG 2.1 AA 베이스라인을 위해 (a) 키보드 사용자가 첫 Tab으로 메인 콘텐츠에 점프 가능, (b) 스크린 리더가 페이지 구조(navigation/main/tabs)를 명확히 안내, (c) 아이콘 전용 컨트롤이 의미 전달, (d) 동적 메시지가 즉시 음성 안내. Python regex 패스 도중 두 파일이 truncation 발생했으나 host Read로 즉시 진단·복구. 9 HTML 전체 무결성 재확인(body open/close, html close, skip-link 1, main-id 1+ 모두 PASS). 다음 라운드 R12: D4 (이미지 lazy-load + critical CSS + 폰트 swap) per D2 proposal §7. |
| 2026-04-25 | [자율 R10] D2-C + D2-D 페이지 CSS 토큰 마이그레이션 (intro + service-detail 잔여) — D2 미션 클리어 | bash sed로 동일 byte-level 패턴 재사용. intro-styles.css(784줄): 색상 77건 → 토큰 77건 + 그림자 9종 마이그레이션, 잔존 **0건**. service-detail-styles.css(755줄): 잔여 색상 92건 → 추가 토큰 65건(R7 chat-input form 60건 포함하여 총 125 토큰) + 그림자 6종 마이그레이션, 잔존 **0건**(focus ring rgb(37 99 235 / 0.15)는 의도적 brand color literal 유지). 색 매핑 보강: `#333`/`#444`/`#555`/`#4b5563` → text-muted, `#fde68a` → warn-bg. **4 페이지 CSS 종합**: styles 47토큰 / intro 77토큰 / all-services 43토큰 / service-detail 125토큰 = **총 292 토큰 / 잔존 hardcoded 0건**. UTF-8 무결성 검증 PASS. diff 합계 598줄 변경 (R9 302 + R10 596). | 사용자 "미비점 개선하면서 계속 진행" 자율 R10. R9에서 입증된 sed 패턴(byte-level, 한국어 안전)을 두 큰 파일에 동일 적용. service-detail은 R7 chat-input form 작업 시 60토큰을 이미 추가했으므로 잔여 92건만 처리. focus ring의 brand-tinted rgba는 의도적 literal로 유지(token화 시 색이 너무 옅어짐). 다음 라운드 R11: D3 (HTML 시맨틱 + ARIA + 키보드 네비). |
| 2026-04-25 | [자율 R9] D2-A + D2-B 페이지 CSS 토큰 마이그레이션 실행 (styles + all-services) | bash sed로 byte-level ASCII 색상 일괄 치환 — 한국어 줄 무영향 (truncation 위험 0). styles.css(488줄): 색상 47건 → 토큰 47건 + 그림자 6 종(rgba→--mk-shadow-xs/sm/md/lg)으로 마이그레이션, 잔존 hardcoded **0건**. all-services-styles.css(412줄): 색상 43건 → 토큰 43건 + 그림자 4종 마이그레이션, 잔존 **0건**. 모든 var() 호출에 fallback hex 포함(--mk-color-text, #1a1a1a 형태)하여 토큰 미정의 시에도 정상 렌더. 색 매핑: #000/#1a1a1a/#1f2937 → text / #374151/#6b7280 → text-muted / #fff → bg / #fafafa/#f8fafc → bg-subtle / #e5e7eb → border / #2563eb/#3b82f6 → brand-500 등 30+ 매핑. UTF-8 무결성 검증 PASS. diff 합계 302줄 변경. | 사용자 "진행" 자율 R9. D2 제안서 §2 우선순위에 따라 D2-A(index)+D2-B(all-services) 동시 처리. 이전 라운드 한국어 truncation 사고 패턴 회피 위해 bash sed의 byte-level 매칭 활용 — `#000\b` 같은 정규식이 한글 multi-byte 시퀀스와 충돌하지 않음. 다음 라운드 R10: D2-C(intro-styles 784줄, hardcoded 77건) + D2-D(service-detail 잔여 92건) 처리. |
| 2026-04-25 | D2 페이지 CSS 토큰 마이그레이션 제안서 작성 (D1 후속) | `_workspace/design/D2_Page_CSS_Token_Migration_Proposal.md` ~200줄 — 4 페이지 CSS(styles/intro/all-services/service-detail) 진단(총 2439줄, hardcoded 색 259건, mk-토큰 60건만 service-detail에 적용됨) + Anthropic ref 색 매핑 표 + spacing/radius/font-size 토큰 매핑 + 미디어 쿼리 7종 → 3 표준 통일 가이드 + 4 단계 우선순위/시간 추정(D2-A index 30분, D2-B all-services 25분, D2-C intro 50분, D2-D service-detail 잔여 30분) + 각 단계 워크플로우 + 위험 요소 5종 + bash 검증 명령 + D3(HTML 시맨틱+ARIA) 예고. | 사용자 "Claude 디자인 시스템 + MaKIT 현황 비교 → D1 제안서" 자율 진행. 현재 시점 진단: D1 토큰은 정의했으나 service-detail 외 3 페이지가 hardcoded 색 그대로 → 시각 일관성 절반만 달성. D2 제안서로 다음 4 라운드(R9~R12) 로드맵 명시. 실 마이그레이션은 R9에서 styles.css부터 시작. |
| 2026-04-25 | [자율 R8] 콘텐츠 CRUD + 채널 metric 실수집 + WebSocket 실시간 알림 push | **backend**: (Group A — 콘텐츠 CRUD) MarketingHubController +4 endpoint (POST/GET/PATCH/DELETE /api/marketing/contents/{id}, 모두 @Auditable). ContentCreateRequest/UpdateRequest record DTO 신설. ContentRepository.findByIdAndUserId 추가. MarketingHubServiceImpl에 createContent/getContent/updateContent/deleteContent 4메서드 (소유권 AccessDeniedException + @Transactional + 상세 로깅). (Group B — 채널 metric 실수집) AuditLogRepository.findChannelMetricsByDay native SQL 추가 (Asia/Seoul timezone, date+resource group). MarketingHubServiceImpl.mapServiceKeyToChannel 매핑 (youtube-* → YOUTUBE / feed/remove-bg/modelshot → INSTAGRAM / nlp/url → SEO / chatbot/review → ADS). getChannelPerformance 완전 구현 — N일 × 4 채널 초기화 + 실 metric 매핑 + 채널별 합계/평균. (Group C — WebSocket) pom.xml에 spring-boot-starter-websocket 추가. WebSocketConfig 신설 — STOMP + SockJS, /ws endpoint, /topic + /user broker, /app prefix. NotificationService 인터페이스 + Impl — JPA 저장 + SimpMessagingTemplate.convertAndSendToUser 동시 호출 (WebSocket 실패해도 DB 보존 graceful fallback). NotificationController에 POST /api/notifications/me/test 데모 endpoint. **frontend**: api.js +4 contents CRUD wrapper (contentGet/Create/Update/Delete). js/pages/marketing-hub-content.js (~120줄) — event delegation [data-content-id] 클릭 시 편집 모달 + "+신규 콘텐츠" 버튼 자동 mount + TYPE 5종(TEXT/IMAGE/VIDEO/FEED/CAMPAIGN_ASSET) select. js/ws-client.js (~110줄) — SockJS+StompJs CDN 활용, /user/queue/notifications 구독, 메시지 도착 시 알림 종 badge/list 실시간 갱신 + ui.toast로 사용자 즉시 알림, 자동 재연결, beforeunload/clearSession에서 disconnect, JWT Bearer header 인증. 7개 protected HTML(index/intro/all-services/service-detail/marketing-hub/history/settings)에 SockJS@1.6.1 + StompJs@7.0.0 + ws-client.js 일괄 include. marketing-hub.html에 marketing-hub-content.js include. | 사용자 "R8 즉시 시작" 자율 진행. R6에서 콘텐츠 라이브러리 GET만 있고 CRUD 부재 → 마케터가 콘텐츠를 보기만 가능. 이번 변경으로 콘텐츠 카드 클릭 편집/삭제, +신규로 추가 가능. R6 채널 성과는 stub(모두 0)이라 차트가 무의미 → audit_logs 실 데이터 기반으로 4채널 시계열 산출, 실제 사용 패턴이 차트에 반영됨. 알림 시스템은 R5에서 polling 방식이라 즉시성 부족 → STOMP push로 신규 알림이 즉시 종 badge 갱신 + toast 표시. WebSocket disconnect/reconnect 자동 처리 + JWT 인증으로 보안 유지. 다음 라운드: D2 페이지 CSS 토큰 마이그레이션 (4 페이지 CSS 2300줄을 D1 토큰 기반으로 재작성). |
| 2026-04-25 | [자율 R7] 캠페인 CRUD UI + 모달 컴포넌트 + AI 인사이트 실 Bedrock 호출 | **backend (Group A — 캠페인 CRUD)**: Campaign entity에 Channel enum(INSTAGRAM/YOUTUBE/SEO/ADS/MULTI) + Status enum 6개(DRAFT/SCHEDULED/ACTIVE/PAUSED/COMPLETED/ARCHIVED) 추가. CampaignRepository.findByIdAndUserId 추가. MarketingHubController에 5 endpoint 추가 (POST /campaigns, GET /campaigns/{id}, PATCH /campaigns/{id}, POST /campaigns/{id}/status, DELETE /campaigns/{id}) — 모두 @Auditable. CampaignCreateRequest/UpdateRequest/StatusChangeRequest 3 record DTO. MarketingHubServiceImpl에 5 CRUD 메서드 + 상태 머신 검증 (DRAFT→SCHEDULED→ACTIVE→{PAUSED,COMPLETED}, COMPLETED/ARCHIVED 종착). Flyway V202604251700__update_campaigns_crud.sql (channel column 추가, ENDED→COMPLETED 마이그레이션, 인덱스). GlobalExceptionHandler에 IllegalStateException → 400 추가. **backend (Group B — Bedrock 인사이트)**: WeeklyInsightService 인터페이스 + Impl 신설. audit_logs.findActivityByDay(userId, 7) 호출하여 사용자별 지난 7일 활동 통계 수집 → 한국어 자연어 정리 → Bedrock Claude Haiku Converse API 호출 → 응답 마크다운 반환. Bedrock 실패 시 stub markdown으로 자동 fallback (source 필드: "bedrock-claude" 또는 "stub-rule-based"). WeeklyInsightController가 stub 직접 생성하던 로직을 service 위임으로 변경. **frontend**: js/modal.js (~140줄) 글로벌 모달 컴포넌트 — open(opts)/close()/confirm({title, message})/prompt({title, defaultValue}) 4 메서드. ESC/backdrop 클릭 닫기. body는 string HTML 또는 DOM Node. actions는 secondary/primary/danger 3 type 버튼. js/pages/marketing-hub-campaign.js (~200줄) — event delegation으로 보드 카드 [data-campaign-id] 클릭 시 api.marketing.campaignGet 호출하여 편집 모달, "신규 캠페인" 버튼 자동 mount, 캘린더 [data-calendar-date] 클릭 시 일자 모달. STATUS/CHANNEL 한국어 라벨 매핑. 5 캠페인 CRUD wrapper 추가 (api.js). app-shell.css +100줄 (.mk-modal-root/.mk-modal-dialog/.mk-modal-head/.mk-modal-body/.mk-modal-actions/.mk-modal-btn--{secondary,primary,danger} D1 토큰 사용 + max-height 60vh + 모바일 width 32px 여백). marketing-hub.html script include 추가 (modal.js + chatbot.js + marketing-hub-campaign.js). | 사용자 "다음 라운드 즉시 시작" + "진행 상황중에 빠진 부분 승인 받지 말고 계속 진행". R6에서 캠페인 GET만 있고 CRUD 부재 → 마케터가 보드를 보기만 가능. 이번 변경으로 카드 클릭하면 편집 모달, "+ 신규 캠페인" 버튼으로 추가, 상태 머신으로 안전한 상태 전환, 삭제 confirm 2단계. AI 인사이트가 stub markdown에서 실제 사용자 데이터 기반 Bedrock Claude 답변으로 격상. modal.js는 다음 라운드의 settings/콘텐츠 라이브러리/리포트 등에서 재사용 가능한 인프라. |
| 2026-04-25 | [자율 R6 - 하네스 통합] 마케팅 허브 신설 ("완벽한 마케팅 지원 플랫폼" 비전) | **신설 (architecture)**: `_workspace/architecture/marketing_hub_master_plan.md` ~180줄 — 5 핵심 영역(캠페인 관리·콘텐츠 라이브러리·캘린더·AI 주간 인사이트·다중 채널 성과) + 데이터 모델 스케치 + 6 backend API 계약 + MVP→완성형 매트릭스. **신설 (frontend)**: `frontend/marketing-hub.html` ~520줄 (요약 카드 4 + 5상태 칸반 보드 + 콘텐츠 라이브러리 그리드 + 7일 캘린더 + AI 주간 인사이트 마크다운 + 4 채널 라인 차트 with Chart.js, D1 토큰 + ARIA + 모바일 반응형) + `frontend/js/pages/marketing-hub.js` ~320줄 (모든 fetch graceful fallback). **신설 (backend hub)**: `marketing/hub/` 11 파일 — MarketingHubController(GET /hub /campaigns /contents /calendar/week /channels/performance) + MarketingHubService 인터페이스+Impl + 6 record DTO(HubSummaryResponse·CampaignDto·ContentDto·CalendarBucket·DailyMetric·ChannelPerformance) + Content entity + ContentRepository + CampaignRepository 보강. **신설 (backend insights)**: WeeklyInsightController GET /api/marketing/insights/weekly — stub markdown 동적 생성 (이번 주 발견사항 + 권장 액션 + 다음 주 우선순위 표 markdown 응답). **수정**: api.js marketing 객체에 hub/campaigns/contents/calendar/insightsWeekly/channelPerformance 6 wrapper. index.html 사이드바 "홈" 섹션에 마케팅 허브 nav-item 추가. app-shell-extras.js PAGES 배열에 marketing-hub 등록 (Cmd+K 검색 즉시 접근). history/404 페이지에 app-shell-extras.js include 추가 (전 페이지 알림 종+Cmd+K+다크모드 일관). | 사용자 "하네스 구성원들이 논의해서 완벽한 마케팅 지원 플랫폼" 자율 진행. architect+backend+ai 3 에이전트 병렬 spawn. 마케팅 허브 = 마케터의 single pane of glass — 캠페인/콘텐츠/일정/인사이트/성과를 한 화면 통합. backend는 stub 데이터로 시작하되 frontend는 graceful fallback으로 즉시 렌더 가능. 다음 라운드: 캠페인 CRUD UI + 캘린더 일자 클릭 모달 + AI 인사이트 실 Bedrock 호출 + 채널별 실 metric 수집. |
| 2026-04-25 | [자율 R5] 알림 센터 + Cmd+K 글로벌 검색 + Settings 페이지 + 다크모드 토글 | **backend** (7 신설 + 4 수정): notifications 도메인 — V202604251500 마이그레이션(notifications 테이블 + INFO/SUCCESS/WARN/ERROR 체크 + 2 인덱스) + Notification entity + Repository(findByUserIdOrderByCreatedAtDesc Page + countByUserIdAndReadAtIsNull + @Modifying markAllRead) + NotificationDto record + NotificationController(GET me/unread-count/POST read-all/POST {id}/read 4 endpoint, 본인 검증). 사용자 프로필 — UpdateProfileRequest + ChangePasswordRequest record + AuthController PATCH /me + POST /change-password + AuthService 두 메서드 추가(이메일 unique + passwordEncoder.matches + IllegalArgumentException("CURRENT_PASSWORD_MISMATCH")). GlobalExceptionHandler IllegalArgumentException → 400. **frontend** (3 신설 + 1 수정): app-shell-extras.js (~280줄) — 다크모드 토글(localStorage makit_theme + data-theme 적용 + window.makitTheme), 알림 종(우상단 fixed 종 아이콘 + unread 빨간 badge + 360px panel + INFO/SUCCESS/WARN/ERROR 색 분기 + relTime 한국어 + readAll 버튼), Cmd+K 검색 palette(Cmd+K/Ctrl+K/"/" 단축키 + 모달 + 입력 필터 + ↑↓ 이동 + Enter jump + Esc 닫기, 10 SERVICES + 6 PAGES 검색 대상). settings.html (~140줄, 프로필 폼+비밀번호 변경 폼+테마 3옵션 라디오, D1 토큰 inline CSS) + js/pages/settings.js — api.auth.updateProfile/changePassword 호출 + 409 한국어 분기 + makitTheme.set. api.js: notifications 4 wrapper + auth.updateProfile/changePassword + dashboard.activity. 4 HTML(intro/all-services/service-detail/index)에 app-shell-extras.js include 추가. user-menu.js의 settings href를 #에서 settings.html로 + 활동 이력 메뉴 항목 신규. css/app-shell.css +160줄(.mk-notif-bell/.mk-notif-panel/.mk-notif-item, .mk-cmdk modal/list/footer/kbd 스타일). | 사용자 R5 자율 진행 명시. 사용자가 자기 프로필을 직접 편집하고 비밀번호를 바꾸며 다크모드를 켜고 끄고 알림을 확인하고 어디서나 Cmd+K로 빠르게 점프하는 일상적 SaaS UX 완성. backend AuthController에 2 endpoint만 추가했고 GlobalExceptionHandler IllegalArgumentException 핸들러 1줄 — 위생적 변경. |
| 2026-04-25 | [자율 R4] AOP audit 자동 INSERT + 챗봇 thumbs 피드백 + dashboard 7일 차트 | **backend**: audit/Auditable annotation + AuditAspect AOP (@Around @annotation+@within, REQUIRES_NEW transaction, status/duration_ms/exception 메타데이터, INSERT 실패해도 비즈니스 logic 계속). 11 controller 메서드에 @Auditable 적용 (DataIntelligence 5 + Marketing 2 + Commerce 3 + ChatbotStream 1 — 각자 service-key resource). pom.xml에 spring-boot-starter-aop 의존성 + MakitApplication에 @EnableAspectJAutoProxy. 챗봇 피드백: V202604251400 마이그레이션 (chatbot_feedback 테이블 + 3 인덱스 + ON DELETE CASCADE) + ChatbotFeedback entity + Repository(countByUserIdAndHelpful) + ChatbotFeedbackRequest record + ChatbotFeedbackController POST /api/commerce/chatbot/feedback (201). 7일 차트: ActivityBucket record(LocalDate, long), DashboardController GET /api/dashboard/activity?days=N (1~30 clamp), DashboardServiceImpl.activityBuckets 구현 (Asia/Seoul timezone native SQL group by day, 빈 날짜 0으로 채움). **frontend**: api.js에 dashboard.activity(days) + commerce.chatbotFeedback(payload) wrapper. chatbot-widget.js의 botMessageShell이 mk-chat-msg-body wrapper로 변경 + done 직후 attachFeedback() 호출하여 thumbs 2 버튼 + status text 마운트. css/app-shell.css +90줄 (.mk-chat-feedback/.mk-chat-fb-btn[--active] 활성 상태 색 분기 + activity-section/.activity-chart-wrap/.activity-link). index.html에 7일 활동 섹션(activity-section + canvas#activityChart) + Chart.js CDN(jsdelivr 4.4.0) script. index.js에 renderActivityChart(buckets): Chart.js bar 차트, brand-500 막대, 한국어 날짜 label, tooltip "X회". | 사용자 "라운드 승인없이 계속 진행" 자율 R4. R3에서 audit_logs INSERT가 LOGIN/REGISTER만이라 dashboard topServices가 비어있던 문제 — AOP로 11개 service controller에 자동 INSERT 적용 → 다음 호출부터 실 데이터 수집. 챗봇 답변 품질 RLHF 데이터 수집 시작 (helpful boolean + comment nullable). 사용자가 7일 단위 자기 활동 패턴을 한눈에 시각화. truncation 발생 — bash heredoc append로 즉시 복구. |
| 2026-04-25 | [자율 R3] audit_logs 실 query + 챗봇 마크다운 + history 페이지 | **backend**: AuditLogRepository에 4 메서드 추가 (countByUserId, findFirstByUserIdAndActionOrderByCreatedAtDesc, findByUserIdOrderByCreatedAtDesc, findTopResourcesByUser JPQL). JobExecutionRepository에 countByUserIdAndStatusIn 추가. DashboardServiceImpl 실 query 교체 (stub → audit_logs/jobs 집계). AuditLogController GET /api/audit-logs/me (페이지네이션 page/size, 기본 20 max 100). AuditLogDto record. SERVICE_KEY_TO_NAME 정적 map 10개 (nlp-analyze→자연어 분석 등). **frontend**: chatbot.js 마크다운 렌더링 추가 (~50줄: bold/italic/inline-code/fenced-code/link/list/단락). 스트리밍 중엔 raw textContent, done 이벤트에서 renderMarkdown(accumulated)로 innerHTML 교체. textHolder를 `<p>`에서 `<div class="mk-md-content">`로 변경. api.js audit.mine() wrapper. css/app-shell.css에 .mk-md-content 스타일 (~40줄: pre/code/list/link D1 토큰). frontend/history.html 신설 (~190줄: header + 4 filter-chip + 활동 list + pagination, 자체 inline CSS). frontend/js/pages/history.js 신설 (~140줄: ALL/LOGIN/SERVICE_CALL/ERROR 필터, page/size 페이지네이션, formatTime 한국어 상대시간, actionLabel/actionInitial 매핑). user-menu.js 드롭다운에 "활동 이력" 메뉴 항목 추가. | 사용자 "다음 라운드 즉시 시작" + "audit_logs 실 query + 마크다운 + history 페이지". 기존 dashboard stub이 자동으로 의미 있는 값 반환하게 됐고, 챗봇 답변이 더 풍부한 표현(목록/코드 블록 등)으로 렌더되며, 사용자가 자기 활동을 추적할 수 있는 페이지 신설. AuditLog 인서트 자체는 별도 (서비스 호출 시 audit_logs INSERT 코드 추가는 다음 라운드 — 현재는 LOGIN/REGISTER만 INSERT됨). |
| 2026-04-25 | [자율 R2] dashboard 통계 API + 챗봇 backend 컨텍스트 인식 + frontend 동적 stats | **backend**: `backend/src/main/java/com/humanad/makit/dashboard/` 신설 (5 파일) — DashboardController(`GET /api/dashboard/stats`) + DashboardStatsResponse(record: userCount/myRequestCount/myJobsInProgress/topServices/lastLoginAt) + ServiceUsageDto(record) + DashboardService 인터페이스 + DashboardServiceImpl(현재 stub: userRepository.count() 외 모두 0/empty, audit_logs/jobs 테이블 query는 향후 보강). Flyway V202604241300__create_dashboard_indexes.sql (4 인덱스: idx_users_active, idx_audit_logs_user_action, idx_jobs_user_status PARTIAL on PENDING/RUNNING, idx_audit_logs_resource). **챗봇 컨텍스트 인식**: ChatbotStreamController에 extractContextHint() 헬퍼 + ContextExtractionResult record 추가하여 `[페이지 컨텍스트: <hint>]\n` 정규식 prefix 추출. ChatbotService.chatStream 4-arg overload (3-arg는 null 위임, backward-compat). ChatbotEngine 인터페이스에 3-arg 추가. RAGChatbotEngine이 hint 비어있지 않으면 system prompt 앞에 `[페이지 컨텍스트] <hint>\n\n` prepend → Bedrock Claude Haiku가 페이지 맥락 인식한 답변. **frontend**: api.js에 dashboard.stats() wrapper. index.js에 dashboard 동적 fetch (api.dashboard.stats then renderStats catch hardcoded fallback). renderStats가 stats-grid의 4 카드에 가입자수/내 요청수/진행중 작업/마지막 로그인 매핑(toLocaleString + 한국어 상대시간 formatDate). | 사용자 "전체 시스템 개선 + 챗봇" + "그만하라고 할 때까지 자율 진행" 모드. 챗봇 컨텍스트 인식은 frontend chatbot-widget이 이미 `[페이지 컨텍스트:]` prefix 보내도록 R1에서 추가됐으므로 backend pairing이 자연스러운 다음 단계. dashboard stats는 hardcoded 마케팅 데모값(94%/54%/123h/2w)을 실제 사용자 데이터(가입자수/요청수/작업수/로그인시간)로 의미 전환. backend stub은 첫 라운드 — 다음 라운드에 audit_logs/jobs 실 query 보강. |
| 2026-04-25 | [자율] 5 HTML 복구 + 글로벌 AI 챗봇 위젯 + user 메뉴 + 404 페이지 (시스템 보강 라운드) | **위기 복구**: 이전 라운드 Edit 도구 한국어 truncation으로 5 HTML 모두 `</body></html>` 부재 상태였음 (login.html은 회원가입 패널 통째 누락) → frontend-engineer subagent가 5 HTML 처음부터 깨끗하게 재작성 (총 ~70KB). **신규 글로벌 컴포넌트 3종**: (a) frontend/js/chatbot-widget.js (~250줄) — 우하단 floating AI 챗봇, makitChatbot.streamInto() SSE 활용, localStorage 30턴 이력 보존, 페이지 컨텍스트 자동 prepend (첫 메시지에 page title + service key hint), 추천 질문 3개, ESC/clear 단축, 미로그인/로그인 페이지 자동 비활성화. (b) frontend/js/user-menu.js (~150줄) — 우상단 프로필 드롭다운, avatar(initials) + 이름 + role 뱃지 + 설정 + 로그아웃 (POST /api/auth/logout → clearSession → /login.html). (c) frontend/css/app-shell.css (~430줄) — 두 컴포넌트 D1 토큰 100% 사용, 다크모드 호환, 모바일 반응형, prefers-reduced-motion 존중. **JS-HTML 동기화**: login.js 전면 재작성하여 새 ID 패턴(loginEmail/loginPassword/regMessage/regBtn/regSpinner/agreeTerms)에 맞춤 + window.doLogin/doRegister 글로벌 노출 (inline onclick 호환). service-detail.js의 selector 6곳을 ID 우선/class fallback 패턴으로 보강 (#serviceTitle, #welcomeTitle, #questionsGrid, #chatMessages, #chatInputContainer 등). **frontend/404.html 신설** — D1 토큰 + 게이밍 SVG illustration + 대시보드/모든서비스/뒤로 3 액션. | 사용자 "전체 시스템 개선 + 기능 추가, 특히 AI 챗봇 추가" + "허가 받지 말고 그만하라고 할 때까지 진행" 자율 모드 요청. 챗봇은 이미 service-detail의 chatbot key로 동작 가능했으나 사용자 명시 요청은 "어디서나 접근 가능한 위젯". user-menu는 명백한 갭 (로그아웃 UI 부재). truncation 복구는 위기 대응이라 동시 처리. 이번 라운드 새 자산은 모두 host filesystem 정상 — sandbox bash mount만 sync 깨져 보임 (이전 README_JA 사고 패턴 동일). |
| 2026-04-25 | [자율 R4] AOP audit_logs 자동 INSERT + 챗봇 thumbs 피드백 + 7일 activity 차트 | **Group A — AOP 기반 audit_logs 자동 INSERT**: audit/Auditable.java annotation 신설 (@Target METHOD, TYPE). AuditAspect.java 전면 업그레이드 — @Around("@annotation(auditable)") + @Around("@within(auditable)") 추가하여 @Auditable 메서드/클래스 모두 자동 audit. 성공/실패 분기, duration_ms + exception metadata 기록, REQUIRES_NEW transaction (비즈니스 롤백시에도 audit 보존). 각 컨트롤러 10 @PostMapping에 @Auditable 추가: DataIntelligence(5: nlp-analyze/youtube-{comments,influence,keyword-search}/url-analyze), MarketingIntelligence(2: feed-generate/remove-bg), CommerceBrain(3: chatbot/review-analysis/modelshot), ChatbotStreamController.stream(). MakitApplication에 @EnableAspectJAutoProxy 추가 (pom.xml에 aop 이미 있음). **Group B — 챗봇 thumbs 피드백**: Flyway V202604251400__create_chatbot_feedback.sql (테이블 생성, 3 인덱스: user/helpful/context). ChatbotFeedback.java entity (id/userId/contextId/messageIdx/helpful/comment/createdAt @PrePersist). ChatbotFeedbackRepository (countByUserIdAndHelpful). ChatbotFeedbackRequest.java DTO record. ChatbotFeedbackController.java (@PostMapping /feedback, userId 추출, 201 Created). **Group C — 7일 activity 시계열**: ActivityBucket.java record (date/count). DashboardService 인터페이스에 activityBuckets(userId, days) 메서드 추가. AuditLogRepository.findActivityByDay(userId, days) native SQL 추가 — `to_char(created_at AT TIME ZONE 'Asia/Seoul', 'YYYY-MM-DD')` 사용하여 일자별 count 그룹핑. DashboardServiceImpl.activityBuckets() 구현 — 오늘부터 거꾸로 N일 범위 생성, 빈 날짜 0으로 채우기 (항상 days개 결과 반환). DashboardController에 GET /api/dashboard/activity endpoint 추가 (@RequestParam days default 7). | 사용자 "자율 진행" 3그룹 동시 구현 요청. Group A는 기존 AuditAspect가 hardcoded @AfterReturning 4개만 있어서 10개 서비스 호출이 audit_logs INSERT 안 됨 → dashboard topServices/myRequestCount 항상 0. @Auditable annotation + generic @Around pattern으로 controller POST mapping 모두 자동 감시. Group B는 사용자 피드백 수집을 위한 thumbs-up/down UI backend 지원 (entity/repo/controller). Group C는 7일 activity 시계열로 frontend dashboard 차트용 데이터 제공. 각 그룹 파일은 self-contained, 의존성 최소 (annotation/entity/repo/dto/controller 각 1개), 이미 있는 인프라(AOP/JPA/Security) 활용. |
| 2026-04-25 | [자율 R7] 캠페인 CRUD + 상태머신 + AI 주간인사이트 Bedrock 보강 | **Group A — Campaign CRUD + State Machine**: Campaign.java enum 확장 (Status: DRAFT/SCHEDULED/ACTIVE/PAUSED/COMPLETED/ARCHIVED 6개 + Channel: INSTAGRAM/YOUTUBE/SEO/ADS/MULTI 5개). Campaign entity에 channel: Enum(Channel) 필드 추가, startDate/endDate OffsetDateTime로 타입 변경. CampaignCreateRequest/CampaignUpdateRequest/CampaignStatusChangeRequest 3 DTO record 신설. CampaignDto에 channel + description 추가. CampaignRepository.findByIdAndUserId(Long, UUID) 메서드 추가. MarketingHubService 인터페이스에 5 CRUD 메서드: createCampaign/getCampaign/updateCampaign(PATCH)/changeCampaignStatus(상태머신)/deleteCampaign 추가. MarketingHubServiceImpl에 상태머신 구현 (Map<Status, Set<Status>> ALLOWED_TRANSITIONS, transition 검증 시 IllegalStateException 발생, 본인 소유 검증 시 AccessDeniedException 발생). MarketingHubController에 5 endpoint 추가 (@Auditable(resource="marketing-campaign", action=CREATE/UPDATE/STATUS_CHANGE/DELETE) 모두 적용). **Group B — Weekly Insight Bedrock 통합**: WeeklyInsightService 인터페이스 신설 (generateWeeklyInsight(userId) → Map<weekStart/weekEnd/markdown/generatedAt/source>). WeeklyInsightServiceImpl 신설 — auditLogRepository.findActivityByDay(userId, 7) 호출, activity 데이터를 한국어 자연어로 정리, Bedrock Claude에 system+user prompt 전송하여 실제 AI 인사이트 생성. Model: anthropic.claude-3-5-haiku-20241022-v1:0, maxTokens: 1024, temperature: 0.7. Bedrock 호출 실패 시 자동 fallback to stub markdown (source: "stub-rule-based"). WeeklyInsightController 기존 stub 코드 제거, SecurityContext에서 userId 추출하여 service 호출. GlobalExceptionHandler에 IllegalStateException 핸들러 추가 (400 BAD_REQUEST). **Database**: V202604251700__update_campaigns_crud.sql migration (channel column 추가, status constraint 확장: SCHEDULED/COMPLETED/ARCHIVED 지원, channel enum 제약조건, 기존 ENDED → COMPLETED 마이그레이션, idx_campaigns_channel 인덱스 추가). | 사용자 "Group A — 캠페인 CRUD + 상태 머신, Group B — AI 주간 인사이트 실 Bedrock 호출" 명시적 요청. Group A는 기존 campaign list 읽기만 가능한 상태에서 마케터가 직접 캠페인 생성/수정/상태변경/삭제 가능하도록 완성. 상태머신 (DRAFT → SCHEDULED → ACTIVE → COMPLETED) 엄격 검증으로 비즈니스 로직 보호. PATCH 메서드로 선택적 업데이트 가능. Group B는 기존 stub markdown (고정 템플릿)을 사용자 실제 활동 데이터 기반 Bedrock 인사이트로 전환. audit_logs의 지난 7일 집계를 인사이트 생성의 context로 사용하여 개인화된 마케팅 조언 제공. Bedrock 장애 시 seamless fallback으로 서비스 가용성 보장. 5 새 엔드포인트 모두 @Auditable로 감시되어 마케터 행동 추적 가능. |
