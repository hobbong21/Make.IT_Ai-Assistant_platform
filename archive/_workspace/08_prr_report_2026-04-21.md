# 08 — Production Readiness Review (PRR)

**Phase**: Production Readiness Review — static verification of Ops Readiness round
**Reviewer**: qa-engineer (read-only)
**Date**: 2026-04-21
**Scope**: 7 boundaries (B1–B7) plus quick hygiene scans
**Method**: Static cross-boundary reading only; no terraform/maven/docker execution

---

## 0. Executive counts

| Boundary | PASS | MINOR | MAJOR | BLOCKER |
|----------|:----:|:-----:|:-----:|:-------:|
| B1. AI ↔ Backend (new deps)          |  3 |  0 |  0 |  2 |
| B2. Backend ↔ Frontend (SSE shape)   |  5 |  1 |  2 |  0 |
| B3. Backend ↔ Infra (env + secrets)  |  4 |  2 |  3 |  2 |
| B4. Terraform internal coherence     |  6 |  2 |  2 |  0 |
| B5. CI/CD pipeline integrity         |  6 |  1 |  0 |  0 |
| B6. Monitoring / alerting sanity     |  3 |  1 |  2 |  0 |
| B7. Secret rotation & fail-fast      |  4 |  2 |  0 |  0 |
| Extra hygiene scans                  |  3 |  2 |  0 |  0 |
| **Total**                            | **34** | **11** | **9** | **4** |

---

## B1. AI ↔ Backend — new dependency coherence

### Evidence

- `backend/src/main/java/com/humanad/makit/ai/bedrock/BedrockHealthIndicator.java:12-13` imports `software.amazon.awssdk.services.bedrock.BedrockClient` and `software.amazon.awssdk.services.bedrock.model.ListFoundationModelsResponse`.
- `backend/src/main/java/com/humanad/makit/ai/bedrock/BedrockService.java:10` imports `io.github.resilience4j.reactor.circuitbreaker.operator.CircuitBreakerOperator` and uses it at line 202.
- `backend/pom.xml` lines 126–157 inventory: `bedrockruntime`, `s3`, `resilience4j-spring-boot3`, `spring-boot-starter-actuator`, `bucket4j_jdk17-core`, `logstash-logback-encoder` — but **NO** `software.amazon.awssdk:bedrock` and **NO** `io.github.resilience4j:resilience4j-reactor`.

### Findings

| ID | Severity | Evidence | Impact | Fix | Owner |
|----|:-------:|----------|--------|-----|-------|
| PRR-001 | **BLOCKER** | `BedrockHealthIndicator.java:12-13`, pom.xml has no `software.amazon.awssdk:bedrock` | Backend will fail to compile — `software.amazon.awssdk.services.bedrock.BedrockClient` is not on classpath. Build breaks on `mvn compile`. | Add `<dependency><groupId>software.amazon.awssdk</groupId><artifactId>bedrock</artifactId><version>${aws.sdk.version}</version></dependency>` | backend-engineer |
| PRR-002 | **BLOCKER** | `BedrockService.java:10,202`, pom.xml has no `resilience4j-reactor` | `CircuitBreakerOperator` is in the reactor artifact (not in `resilience4j-spring-boot3`). Compile fails. | Add `<dependency><groupId>io.github.resilience4j</groupId><artifactId>resilience4j-reactor</artifactId><version>${resilience4j.version}</version></dependency>` | backend-engineer |
| PRR-003 | PASS | pom.xml:60-62 | `spring-boot-starter-actuator` present — HealthIndicator auto-registration will work once PRR-001 is resolved. | — | — |
| PRR-004 | PASS | `application.yml:63` `enabled: ${BEDROCK_ENABLED:${AI_BEDROCK_ENABLED:true}}`; `MockBedrockService.java:33` `@ConditionalOnProperty(name="aws.bedrock.enabled", havingValue="false")`; `BedrockConfig.java:21` has `matchIfMissing=true` for real. | Mock mode wiring is coherent: `aws.bedrock.enabled=false` disables real clients & activates mock. `RAGChatbotEngine.resolveStream` handles both. | — | — |
| PRR-005 | PASS | Actuator endpoint exposure in `application-prod.yml:47-50` includes `health` — `BedrockHealthIndicator` discovered as `bedrock` sub-health under `/actuator/health`. Show-details is `never`, so no internal info leaks even on DOWN. | — | — | — |

---

## B2. Backend ↔ Frontend — SSE streaming shape

### Evidence

- Controller: `backend/src/main/java/com/humanad/makit/commerce/chatbot/ChatbotStreamController.java`
- Engine: `backend/src/main/java/com/humanad/makit/ai/rag/RAGChatbotEngine.java:126-176`
- Service: `backend/src/main/java/com/humanad/makit/commerce/chatbot/ChatbotService.java:57-70`
- DTO: `backend/src/main/java/com/humanad/makit/ai/dto/ChatStreamChunk.java`
- FE consumer: `frontend/js/pages/chatbot.js`

### Findings

| ID | Severity | Evidence | Impact | Fix | Owner |
|----|:-------:|----------|--------|-----|-------|
| PRR-006 | **MAJOR** | `ChatbotStreamController.java:60` — `return Flux.merge(events, heartbeat).takeUntilOther(body.ignoreElements());` re-subscribes to `body` a second time. `ChatbotService.chatStream` (lines 57–70) performs side effects before returning: `resolveContext` (DB read/write), `persistMessage(USER, ...)` (DB insert). Each SSE request therefore triggers **two** user-message rows and **two** Bedrock invocations. | Doubles DB writes and Bedrock spend per chat turn; risk of pgvector IVFFLAT hot spot; cost of Bedrock doubles. | Rewrite as a `Flux.create` / `Flux.using` where `body` is shared (`.publish().refCount()` or `.share()`), or gate heartbeat via `.takeUntilOther(body.materialize().takeLast(1))` against the same shared source. | backend-engineer |
| PRR-007 | MAJOR | FE expects `payload.contextId` inside `done` JSON (`chatbot.js:88`). BE emits JSON (ChatbotStreamController.injectContextId adds it). OK. But `delta` events: RAGChatbotEngine emits `ChatStreamChunk(delta, <plain token text>)` — controller wraps as `data: <text>`. FE parses with `JSON.parse(dataLine)` and only falls back to treating it as plain text when parse fails. For tokens that happen to be valid JSON (`"true"`, `42`, `{"a":1}` — legit Korean/English tokens usually aren't, but e.g. punctuation `"`, numerals, or code-like fragments can trigger this), FE will mis-interpret as a structured payload and drop the text. | Intermittent missing tokens in UI. Silent data loss on SSE. | Either wrap each `delta` as `data: {"data":"..."}` on the BE (invert current FE-first design so BE always emits JSON), **or** in FE: when `eventName==='delta'`, skip `JSON.parse` and take the raw `dataLine` as text. | backend-engineer + frontend-engineer (pick one end) |
| PRR-008 | PASS | `ChatStreamChunk.java:15-20` defines the five event types `delta`, `citation`, `done`, `error`, `ping`. Controller emits all five types. | Contract match. | — | — |
| PRR-009 | PASS | `ChatbotStreamController.java:58-59` heartbeat interval `Duration.ofSeconds(15)`. Meets the ≥15s requirement flagged by frontend-engineer earlier. | — | — | — |
| PRR-010 | PASS | `RAGChatbotEngine.java:165-168` — `done` chunk carries `{"contextId":"…"}` before leaving the engine; controller also enforces via `injectContextId` (belt + suspenders). FE reads `payload.contextId` (`chatbot.js:88`). | — | — | — |
| PRR-011 | PASS | Adapter from `Flux<ChatStreamChunk>` to `ServerSentEvent<String>` is straight map (lines 47–57). Event names are emitted via `.event(chunk.event().name())` which is the enum's lowercased name per convention. | — | — | — |
| PRR-012 | PASS | `RAGChatbotEngine.chatStream` onErrorResume emits an `error` chunk then completes — contract-compatible. FE handles `ev==='error'` (chatbot.js:90-92). | — | — | — |
| PRR-013 | MINOR | `ChatbotStreamController.java:91-94` catches `UUID.fromString` failure and falls back to `UUID.randomUUID()` — when an unauthenticated principal reaches this controller in prod somehow, a random UUID becomes the caller identity, not a 401. Security config SHOULD block unauthenticated access to `/api/commerce/chatbot/**`. | If SecurityConfig was misconfigured, anonymous users would stream. Defense-in-depth miss. | Replace fallback with `throw new AuthenticationCredentialsNotFoundException(...)` so the filter chain sees a 401. | backend-engineer |

---

## B3. Backend ↔ Infra — env vars + secrets

### Evidence

- `infra/terraform/modules/ecs/main.tf:166-180` (env + secrets blocks)
- `backend/src/main/resources/application-prod.yml` (prod overlay — only 2 env-referenced values)
- `backend/src/main/resources/application-prod-aws.yml` (explicit secrets wiring)
- `backend/src/main/resources/application.yml` (master defaults)
- `infra/terraform/main.tf:132-169` (ECS module wiring — sets `environment` input to `var.environment` which is "prod")

### Findings

| ID | Severity | Evidence | Impact | Fix | Owner |
|----|:-------:|----------|--------|-----|-------|
| PRR-014 | **BLOCKER** | `ecs/main.tf:179` injects secret as env name `SPRING_REDIS_PASSWORD`, but `application.yml:42-45` reads `spring.data.redis.password: ${REDIS_PASSWORD:}` — relaxed binding translates `SPRING_DATA_REDIS_PASSWORD` to that property, **not** `SPRING_REDIS_PASSWORD` (which would map to deprecated Spring Boot 2 `spring.redis.password`). `REDIS_PASSWORD` is not injected anywhere by Terraform. Prod Redis with `transit_encryption_enabled=true` and `auth_token` REQUIRES the client to send AUTH — the app will connect but every Redis command will fail with `NOAUTH Authentication required`. | Redis entirely broken in prod. Session-ish features (Redis cache, jti blacklist, Bucket4j v1.1 Redis path) all fail. | Change ECS secrets block: `{ name = "SPRING_DATA_REDIS_PASSWORD", valueFrom = var.redis_auth_token_secret_arn }` (plus keep `REDIS_PASSWORD` alias if anything else reads it). | devops-engineer |
| PRR-015 | **BLOCKER** | RDS parameter group has `rds.force_ssl=1` (`modules/rds/main.tf:65-69`). ECS task env `SPRING_DATASOURCE_URL = jdbc:postgresql://${var.db_endpoint}/${var.db_name}` (`modules/ecs/main.tf:169`) contains no `?sslmode=require`. | JDBC connection will be refused by RDS. Backend fails to start. | Append `?sslmode=require` to the URL: `jdbc:postgresql://${var.db_endpoint}/${var.db_name}?sslmode=require`. Optional: also set `spring.data.redis.ssl.enabled=true` for prod Redis (paired with PRR-014). | devops-engineer |
| PRR-016 | MAJOR | Prod Redis uses `transit_encryption_enabled=true` (`modules/elasticache/main.tf:94`). Spring Boot Lettuce client requires explicit `spring.data.redis.ssl.enabled=true` to speak TLS; otherwise it opens a plaintext socket and the server drops it. Not set anywhere. | Redis connect fails in prod. Same impact as PRR-014; also fails when PRR-014 is fixed. | Add `spring.data.redis.ssl.enabled: true` under the prod profile (e.g. into `application-prod-aws.yml`). | backend-engineer + devops-engineer |
| PRR-017 | MAJOR | `CORS_ALLOWED_ORIGINS` is not set in the ECS env block (`modules/ecs/main.tf:166-174`). The default in `application.yml:142` is `http://localhost,...` — prod browser origin will be rejected. | All cross-origin requests from the real production domain fail with CORS preflight rejection. Site unusable for users hitting the frontend ALB directly (if frontend is on a different host) or any third-party embeddable flow. | Add `{ name = "CORS_ALLOWED_ORIGINS", value = "https://${var.public_domain}" }` to ECS env block. Add a `public_domain` input and populate in prod.tfvars. | devops-engineer |
| PRR-018 | MAJOR | `JWT_ISSUER`, `JWT_AUDIENCE` not in ECS env block. `application.yml:56-57` defaults are `https://makit.example.com` / `makit-web`. `JwtTokenProvider.java:108` hardcodes `.requireIssuer(issuer)` — if a rotating op tries to tune the issuer to match the real domain later, tokens from new issuer will fail verification against the old default stored in task-def-baked config. Also tokens carry an incorrect `iss` claim in prod. | Low-grade correctness issue; rotations/migrations will break. Not a compile-time or boot-time failure. | Add `JWT_ISSUER`/`JWT_AUDIENCE` to ECS env block and prod.tfvars. | devops-engineer |
| PRR-019 | MINOR | `SPRING_PROFILES_ACTIVE` set only to `var.environment` i.e. `"prod"` (`modules/ecs/main.tf:167`). `application-prod-aws.yml` is a profile overlay with useful extra config (source: `02_backend_secrets_integration.md`) that will never activate. | The overlay is dead code today. Functional impact is nil because application-prod.yml already references the env-var secrets. | Either set `value = "prod,prod-aws"` in ECS task def, or delete `application-prod-aws.yml` to reduce confusion. | devops-engineer |
| PRR-020 | MINOR | `REDIS_PORT` env in ECS is hardcoded `"6379"` string. Redis side also listens 6379. Functional, but redundantly typed in two places. | None — cosmetic. | Drive from `module.elasticache.port` output. | devops-engineer |
| PRR-021 | PASS | JWT_SECRET, DB_PASSWORD, SPRING_DATASOURCE_PASSWORD all injected via `secrets:` array from the three Secrets Manager ARNs. Pattern matches `02_backend_secrets_integration.md` guidance. | — | — | — |
| PRR-022 | PASS | `AWS_REGION`, `S3_BUCKET`, `SPRING_DATASOURCE_URL`, `SPRING_DATASOURCE_USERNAME`, `SPRING_REDIS_HOST` all wired correctly and consumed by application.yml placeholders. | — | — | — |
| PRR-023 | PASS | `BEDROCK_ENABLED` not injected, defaults to `true` — correct for prod (live Bedrock). | — | — | — |
| PRR-024 | PASS | S3 bucket name output `module.s3_assets.bucket_name` wired into ECS env `S3_BUCKET`, used by `DefaultS3ImageUploader` and `BedrockProperties.s3.bucket`. | — | — | — |

---

## B4. Terraform internal coherence

### Findings

| ID | Severity | Evidence | Impact | Fix | Owner |
|----|:-------:|----------|--------|-----|-------|
| PRR-025 | MAJOR | `modules/iam/main.tf:79-91` Task Role only grants `bedrock:InvokeModel` + `bedrock:InvokeModelWithResponseStream`. `BedrockHealthIndicator` calls `bedrock:ListFoundationModels` (control plane). No IAM statement for it. | `/actuator/health/bedrock` returns DOWN with `AccessDeniedException`. The top-level `/actuator/health` will then return 503 (component DOWN), causing ALB target-group health checks to flap — possible service rollback loop. | Add a statement: `actions = ["bedrock:ListFoundationModels"]`, `resources = ["*"]`. | devops-engineer |
| PRR-026 | MAJOR | `modules/monitoring/main.tf:140-155` ElastiCache alarm uses dimension `CacheClusterId = var.redis_cluster_id`. For prod (`replication_enabled=true`), `module.elasticache.cluster_id` returns the **replication group id** (`aws_elasticache_replication_group.rg[0].id`), not a cluster node id. CloudWatch publishes Redis metrics per `CacheClusterId=<node-id>` (e.g. `makit-prod-redis-001`, `-002`), not by replication group id. | Alarm never fires (no data), alarm state = INSUFFICIENT_DATA forever. `treat_missing_data = "notBreaching"` silences the alerting you wanted. | For prod path: iterate over `member_clusters` output of the replication group, create one alarm per node; OR switch alarm to the `AWS/ElastiCache` metric with `ReplicationGroupId` + `Role=primary` dimensions. | devops-engineer |
| PRR-027 | MINOR | `modules/ecs/main.tf:148` ALB listener rule patterns `["/api/*", "/actuator/*", "/swagger-ui/*", "/v3/api-docs/*"]`. Prod exposure of Swagger UI + v3/api-docs is typical security reduction. Actuator already exposes only 4 endpoints so that part is fine. | Small attack surface — Swagger UI enumerates every endpoint. Probably intended for internal debugging; still. | Drop Swagger routes from the ALB rule in prod, or gate behind internal-only path. | devops-engineer |
| PRR-028 | MINOR | `modules/secrets/main.tf:51-59` — the `aws_secretsmanager_secret_version.jwt` has `lifecycle.ignore_changes=[secret_string]`. If someone rotates out-of-band, Terraform won't notice; good. But **the data source** `data.aws_secretsmanager_secret_version.db_password` in `modules/rds/main.tf:13-15` **does** read the current value at plan time and threads it into `aws_db_instance.this.password`. With `ignore_changes=[password]` on the RDS resource that's harmless on subsequent applies, but the in-state Terraform plan diff exposes the password. | No actual leak (sensitive=true hides most), but first-time operators see `random_password.db[0].result` referenced by RDS — confusing. | Harden with a CustomResource/Lambda for in-RDS rotation, or accept the pattern and document. | devops-engineer (future) |
| PRR-029 | PASS | Security group graph: ALB (0.0.0.0/0 on 80,443) → ECS-backend (from ALB SG only on 8083) → RDS (from backend SG on 5432) / Redis (from backend SG on 6379). ECS → Bedrock/S3 is via egress 0.0.0.0/0 (through NAT), which works. | — | — | — |
| PRR-030 | PASS | RDS parameter group includes `shared_preload_libraries=pg_stat_statements` and `rds.force_ssl=1`; pgvector ships with RDS PG 15 via `CREATE EXTENSION vector` (flyway V1 migration applies it — verify by reading V1). Terraform approach is correct per AWS docs. | — | — | — |
| PRR-031 | PASS | ECS task-def `secrets:` ARNs are wired through module outputs (`jwt_secret_arn`, `db_password_arn`, `redis_auth_token_arn`) → ECS module inputs → container def. Task Execution Role has `secretsmanager:GetSecretValue` on `makit/*` pattern. | — | — | — |
| PRR-032 | PASS | ALB listener HTTPS path: if `certificate_arn` present, HTTP redirects to HTTPS (301); HTTPS listener forwards to frontend; listener rule priority 10 forwards `/api/*` + actuator/swagger to backend target group. Rule attached to HTTPS listener when cert exists, HTTP otherwise. | — | — | — |
| PRR-033 | PASS | Target group health checks: backend `/actuator/health` on 8083; frontend `/healthz` on 80. Backend path matches Spring Actuator default endpoint; frontend path requires nginx to serve `/healthz` (verified in earlier rounds). | — | — | — |
| PRR-034 | PASS | Module outputs referenced by downstream modules all exist: network (vpc/subnets/SG), ecr (repo urls map), secrets (ARNs), iam (role ARNs), s3 (bucket ARN+name), rds (endpoint+instance_id), elasticache (primary_endpoint+cluster_id), ecs (alb_dns, target group arn_suffix, log_group_names, service/cluster names). | — | — | — |

---

## B5. CI/CD pipeline integrity

### Findings

| ID | Severity | Evidence | Impact | Fix | Owner |
|----|:-------:|----------|--------|-----|-------|
| PRR-035 | PASS | `.github/workflows/docker-publish.yml:22` outputs `image_tag: ${{ github.sha }}`; downstream jobs use `${{ needs.build-and-push.outputs.image_tag }}` in `-var=backend_image_tag=...`. Propagation correct. | — | — | — |
| PRR-036 | PASS | staging uses `environment: staging`, prod `environment: prod` — GitHub Environment approvers guard both. Runbook §Prerequisites asks user to set ≥1 / ≥2 reviewers. | — | — | — |
| PRR-037 | PASS | Required secrets: `AWS_ROLE_ARN`, `ECR_REGISTRY`, `TF_STATE_BUCKET`, `TF_STATE_LOCK_TABLE` — all referenced; listed in `07_devops_ops_progress.md` prerequisites. | — | — | — |
| PRR-038 | PASS | State key separation per env: `makit/dev/terraform.tfstate`, `makit/staging/...`, `makit/prod/...`. | — | — | — |
| PRR-039 | PASS | Smoke test for dev (`http://$ALB/healthz`, retry 30× @10s). Staging / prod lack smoke steps — intentional (manual verify gate). | — | — | — |
| PRR-040 | PASS | Prod job uses `terraform plan -out=prod.plan` followed by `terraform apply prod.plan` — plan artifact path as required. | — | — | — |
| PRR-041 | PASS | `backend-test.yml:15` uses `pgvector/pgvector:pg15` matching prod. JWT_SECRET is 47 chars (`test-secret-at-least-32-characters-long-for-hs256`), satisfies the prod length check used in shared JwtTokenProvider boot. | — | — | — |
| PRR-042 | MINOR | `backend-test.yml` sets `SPRING_PROFILES_ACTIVE` implicitly (not set) — meaning Bedrock enabled defaults to `true`, which will try `DefaultCredentialsProvider` during context startup for any test that loads the full app. Tests that don't use Bedrock beans are fine; `@SpringBootTest` tests are not. | Potential flakiness in CI when `AWS_REGION` is present but no creds. | Set `SPRING_PROFILES_ACTIVE: mock` and `AWS_BEDROCK_ENABLED: false` to force MockBedrockService and skip AWS SDK client creation. | backend-engineer + devops-engineer |

---

## B6. Monitoring / alerting sanity

### Findings

| ID | Severity | Evidence | Impact | Fix | Owner |
|----|:-------:|----------|--------|-----|-------|
| PRR-043 | **MAJOR** | `modules/monitoring/main.tf:212-225` alarm references `namespace="MaKIT/Bedrock"`, `metric_name="DailyCostUSD"`. grep across `backend/` for `PutMetricData`, `CloudWatchMeterRegistry`, `DailyCostUSD` returns **zero matches**. `BedrockService.java:480` emits a Micrometer counter `bedrock.cost.usd` but only to the Prometheus registry; there is no CloudWatch registry bean. Dashboard widget `MaKIT/Bedrock TokensTotal` is in the same bucket. | Cost alarm never fires (no data) and is silenced by `treat_missing_data=notBreaching`. The cost guardrail listed as a reason to approve prod is not actually functional. | Either add `micrometer-registry-cloudwatch2` dependency + a bean that publishes `bedrock.cost.usd` as `DailyCostUSD` in namespace `MaKIT/Bedrock` on an hourly schedule, OR remove the alarm until a publisher exists and use AWS Cost Explorer / Budgets for cost cutoffs. | ai-engineer (publisher) + devops-engineer (alarm rewrite) |
| PRR-044 | MAJOR | (Covered by PRR-026.) Redis CPU alarm uses wrong dimension for the prod replication group. | — | — | — |
| PRR-045 | PASS | ALB 5xx rate, ALB p95 latency, RDS CPU, RDS storage, ECS desired-vs-running alarms all use AWS-published metric names with correct namespaces/dimensions. | — | — | — |
| PRR-046 | PASS | SNS topic + email subscriptions via `for_each` over `alarm_email_subscribers`. | — | — | — |
| PRR-047 | MINOR | `bedrock_daily_cost_usd_threshold = 100` (prod.tfvars:46) — $100/day on Bedrock is easily breachable with Claude Sonnet-heavy workloads. Arbitrary default without business approval. | Either false-alarms or too-late-alarms depending on real traffic. | Pair with team — set to a deliberately-chosen value and document. | devops-engineer (config) |

---

## B7. Secret rotation & fail-fast

### Findings

| ID | Severity | Evidence | Impact | Fix | Owner |
|----|:-------:|----------|--------|-----|-------|
| PRR-048 | PASS | `JwtTokenProvider.java:44-51` — prod-profile detection (`getActiveProfiles().anyMatch(p -> p.startsWith("prod"))`) combined with `secret.length() < 32` throws `IllegalStateException` with the exact message required. Log line logs only the length, never the value. | — | — | — |
| PRR-049 | PASS | `DemoUserSeeder.java:27` `@Profile({"dev", "docker", "mock"})` — no `prod` in the list. Will not run in production. Uses PasswordEncoder bean (BCrypt(12)) so no hash drift. | — | — | — |
| PRR-050 | PASS | `RateLimitFilter.java:52-76` — `shouldNotFilter=true` for every request whose URI is not one of the three `/api/auth/*` paths. Actuator health endpoints (`/actuator/health`, `/actuator/health/liveness`, `/actuator/health/readiness`) bypass the filter entirely. | — | — | — |
| PRR-051 | PASS | Logback `prod` profile encoder (`logback-spring.xml:33-56`) uses logstash JSON with MDC but does not explicitly mask tokens. BedrockService-level logging logs only exception class names, not payloads (`BedrockService.java:490`). JwtTokenProvider logs length only, not the secret. | — | — | — |
| PRR-052 | MINOR | `logback-spring.xml` has no regex-based redactor for stray `Authorization:` or `Bearer …` substrings in log messages. Relies on dev discipline. If any future `log.info("req=" + request)` happens it could leak. | Low risk today because no such lines exist; forward-safety concern only. | Add a Logback replacement rule or a custom converter that masks `Bearer [A-Za-z0-9._-]+` → `Bearer ***`. | backend-engineer (later) |
| PRR-053 | MINOR | JwtTokenProvider pads short non-prod secrets to 32 bytes (`:59-64`). A dev typo of a 4-char secret silently works. Combined with local-only profiles it's fine, but any dev token written in dev is effectively low-entropy. | Negligible in non-prod. | Log a WARN when padding is applied. | backend-engineer |

---

## Extra hygiene scans

| ID | Severity | Evidence | Notes |
|----|:-------:|----------|-------|
| PRR-054 | PASS | `grep -r "System\.out\|System\.err" backend/src/main` → 0 results. | — |
| PRR-055 | PASS | No `.env` file present in working tree (only `.env.example`). | — |
| PRR-056 | PASS | No hardcoded `password123` in prod code paths (only in `DemoUserSeeder.java` which is profile-gated to non-prod; and in `frontend/login.html` which is a dev hint). | — |
| PRR-057 | MINOR | 7 `TODO(ai-engineer)` markers in commerce/review, marketing/feed, marketing/image, data/youtube, data/url services. Pre-existing placeholder implementations, will run with mock-ish behavior in prod. | Track as v1.1 backlog — not a PRR blocker since they do not crash or throw. |
| PRR-058 | MINOR | `frontend/login.html` appears to include the demo password visibly in rendered form (source contains "password123"). Acceptable for dev login hint; if kept in production FE bundle it's security theater. | Remove or gate with env flag on frontend build. |

---

## Appendix — verified file inventory

- Backend ops round:
  - `backend/src/main/java/com/humanad/makit/auth/DemoUserSeeder.java`
  - `backend/src/main/java/com/humanad/makit/auth/RateLimitFilter.java`
  - `backend/src/main/java/com/humanad/makit/auth/JwtTokenProvider.java`
  - `backend/src/main/java/com/humanad/makit/config/DefaultS3ImageUploader.java` (not re-read; trust prior QA)
  - `backend/src/main/java/com/humanad/makit/common/LoggingMdcFilter.java` (not re-read)
  - `backend/src/main/resources/application-prod.yml`
  - `backend/src/main/resources/application-prod-aws.yml`
  - `backend/src/main/resources/application.yml`
  - `backend/src/main/resources/logback-spring.xml`
  - `backend/pom.xml`
- AI ops round:
  - `backend/src/main/java/com/humanad/makit/ai/bedrock/BedrockService.java`
  - `backend/src/main/java/com/humanad/makit/ai/bedrock/MockBedrockService.java`
  - `backend/src/main/java/com/humanad/makit/ai/bedrock/BedrockConfig.java`
  - `backend/src/main/java/com/humanad/makit/ai/bedrock/BedrockHealthIndicator.java`
  - `backend/src/main/java/com/humanad/makit/ai/bedrock/BedrockClient.java`
  - `backend/src/main/java/com/humanad/makit/ai/rag/RAGChatbotEngine.java`
  - `backend/src/main/java/com/humanad/makit/ai/dto/ChatStreamChunk.java`
- Backend glue:
  - `backend/src/main/java/com/humanad/makit/commerce/chatbot/ChatbotStreamController.java`
  - `backend/src/main/java/com/humanad/makit/commerce/chatbot/ChatbotService.java`
- Frontend:
  - `frontend/js/pages/chatbot.js`
- Infra:
  - `infra/terraform/main.tf`
  - `infra/terraform/variables.tf`
  - `infra/terraform/modules/network/main.tf`
  - `infra/terraform/modules/ecs/main.tf` + `variables.tf` + `outputs.tf`
  - `infra/terraform/modules/iam/main.tf`
  - `infra/terraform/modules/secrets/main.tf` + `outputs.tf`
  - `infra/terraform/modules/rds/main.tf` + `outputs.tf`
  - `infra/terraform/modules/elasticache/main.tf` + `outputs.tf`
  - `infra/terraform/modules/monitoring/main.tf` + `variables.tf`
  - `infra/terraform/envs/prod.tfvars`
- CI/CD:
  - `.github/workflows/docker-publish.yml`
  - `.github/workflows/backend-test.yml`
